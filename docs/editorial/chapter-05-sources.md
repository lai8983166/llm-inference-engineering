# 第 05 章来源与编辑记录

## 使用规则

本章把第 02 章的执行选择问题组装成可推导的调度循环：决定点、可运行集合、逐拍成员重组与策略竞争。正文中的技术陈述按四类管理：稳定调度原理需要能够追溯到论文或一手文档；框架实现示例必须记录仓库、版本/commit 与核对日期，并只用于“实现示例”而不是“概念定义”；真实延迟、吞吐与公平性判断只能由对应软硬件上的测量支持；确定性教学模型假设只用整数逻辑拍，不以引用把假设包装成事实。

## 稳定调度原理

| 待支持主张 | 一手来源 | 可以支持什么 | 不能推出什么 |
| --- | --- | --- | --- |
| 以迭代（而非整请求）为粒度调度：新请求可以在每个解码迭代后注入、完成的请求可以同时移出，无需等待整批结束；这种机制被称为 iteration-level scheduling，并由此形成 continuous batching | [Orca: A Distributed Serving System for Transformer-Based Generative Models](https://www.usenix.org/conference/osdi22/presentation/yu)，Yu 等，OSDI 2022，2026-08-27 经检索核对 | 逐拍重组成员资格是已发表服务系统采用的调度机制；它针对请求级批处理的 padding 与封闭成员问题 | 论文的吞吐/延迟倍数（对 FasterTransformer 的对比）是其实验条件下的测量结论，不能移植到本章教学轨迹 |
| 连续批处理消除的是请求级批处理中固定成员造成的等待与 padding 瓶颈 | 同上 | 本章第 3 节对照封闭批次与逐拍重组的推导方向与论文动机一致 | 不能推出任何真实系统的具体等待时间或资源占用 |
| 逐拍决定的必要性（执行边界是决定点、可运行集合需重算） | 本章第 1、2 节的推导（延续第 01—04 章已验收合同） | 决定点、六项可运行输入与原因分类均可由前四章合同重组得到 | 教学推导不是引用事实；真实调度器的输入集合更大（如优先级、超时、显存水位） |

## 版本锁定的框架实现示例

| 待支持主张 | 一手来源 | 可以支持什么 | 不能推出什么 |
| --- | --- | --- | --- |
| vLLM 引擎核心进程运行一个持续调度的忙循环（busy loop that continuously schedules requests），“调度”被描述为“选择每一步处理哪些请求” | [vllm-project/vllm `docs/design/arch_overview.md`](https://github.com/vllm-project/vllm/blob/main/docs/design/arch_overview.md)，main@`eac636a`（2026-08-19），2026-08-27 核对 | 真实框架确实存在逐拍选择请求的引擎循环结构，与本章决定点对应 | 不规定其调度器内部策略、优先级、抢占或准入实现；本章不引用其行为作性能结论 |

SGLang 的调度器、chunked prefill 与抢占实现本章不陈述。后续正文若映射更多实现，必须在本表追加：仓库、commit、核对日期与“仅实现示例”边界。

## 必须实测的判断

以下问题在本章只能提出观察需求，不能由逻辑拍给出结论：

- 不同调度策略对真实 TTFT、逐 token 间隔、尾延迟、吞吐和 goodput 的影响。
- prefill 与 decode 在真实硬件上混合执行的形状、成本与干扰。
- 真实调度器的准入、抢占与优先级在具体负载下的行为。
- 教学拍数与任何真实时间量纲的换算关系（不存在这种换算）。

真实结论必须沿用第 00 章的测量合同：固定请求集合与生成工作量，记录观察者和完成事件，保留原始事件、失败与环境信息，再限定结论适用范围。

## 确定性教学模型假设

任务组 1 的调度领域模型只用于形成可复核的因果轨迹：

- 模型、请求与块池完全延续：128 bytes/token、`R-long`/`R-short`/`R-late`、6 块 × 4 unit；准入按第 04 章规则（prompt+1 token 所需块数 ≤ 空闲块）。
- 每拍至多一份设备工作：拍内顺序为到达与准入检查 → 计算可运行集合 → 按策略选择 → 执行 → 完成者释放块。这是教学抽象；真实系统一次执行可混合多种工作（如 chunked prefill），留作实现示例之外的未讲内容。
- decode 组把全部可 decode 成员合为一次执行，每人 1 个有效 token，无 padding；prefill 一次执行一个新请求。
- 两种策略：prefill 优先（新请求 prefill 先于 decode 组，FIFO）与 decode 优先（decode 组先于等待中的 prefill）。
- 逻辑拍是离散事件刻度，不对应毫秒、GPU cycle 或等长执行区间；字段名与界面禁止出现耗时、带宽或利用率语义。

## 编辑记录

### 2026-08-27 · 事件底座

- 已建立四类来源边界，经检索核对 Orca 论文（OSDI 2022）与 vLLM `docs/design/arch_overview.md`（main@`eac636a`）；Orca 的性能倍数只登记为其实验条件下的测量结论。
- 本章概念（决定点、可运行集合、连续批处理）由正文推导命名；机制出处与实现示例只出现在版本锁定表中。
- 教学数据明确使用 `simulated` 和整数逻辑拍，禁止字段名或界面暗示真实耗时、延迟或 GPU 利用率。

### 2026-08-28 · Prose-only 连续正文评审

- 评审时未挂载任何动态图、逐拍实践或迁移评估；仅凭决定点形式化、六项合同清单、逐拍手算表与双策略时间线，已经可以从“下一次执行该选谁”连续推到调度合同清单与容量压力的下一章入口。
- 五个标题只对应认知任务改变：决定点形式化、可运行合同、成员重组与连续批处理命名、双策略竞争、合同收束。没有另设“概念解释”“工程视角”“优缺点”“小结”等复述标题。
- “静态 batch 是沿用上次答案的特例”写在任何机制名称之前；**连续批处理**的命名晚于逐拍手算表，iteration-level scheduling 与真实引擎忙循环只作印证（“不是它的出处”）。
- 双策略时间线逐格手算并与 `buildScheduleTrace`/`buildStrategyTrace('static-batch')` 一致：封闭批次首执行 1/1/5（总 7 拍）、prefill 优先 0/1/3（总 6 拍）、decode 优先 0/4/5（总 7 拍）；正文明确“拍不是时间”、6 对 7 拍不能换算成“快 17%”，等待迁移是结论，不评优劣。
- 原因分类（未到达、待 prefill、等待块、已完成）先于策略讨论出现，由内容测试锁定；容量误诊为调度问题的分界在两处显式陈述。
- 证据边界：拍数、状态与等待只在教学规则内成立；未出现 GB/s、利用率、TTFT、ITL；毫秒仅出现于“一拍不等于一毫秒”的否定句。
