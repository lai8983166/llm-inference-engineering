# 第 08 章来源与编辑记录

## 使用规则

本章把第 00—07 章攒下的原始事件变成指标层：定义、分布、环式与口径。正文中的技术陈述按四类管理：稳定统计与负载原理需要能够追溯到一手文献；框架实现示例必须记录仓库、版本/commit 与核对日期，并只用于“实现示例”而不是“概念定义”；真实延迟、吞吐与 SLO 数值只能由对应软硬件上的测量支持；确定性教学模型假设只用整数逻辑拍与最近邻秩分位，不以引用把假设包装成事实。

## 稳定统计与负载原理

| 待支持主张 | 一手来源 | 可以支持什么 | 不能推出什么 |
| --- | --- | --- | --- |
| 延迟变异在大规模服务中不可忽略；扇出到多台服务器的请求由最慢组成者支配，因此面向用户的服务由高分位（而非均值）刻画 | [The Tail at Scale](https://dl.acm.org/doi/10.1145/2408776.2408794)，Dean & Barroso，Communications of the ACM 56(2)，2013，2026-09-02 经 ACM DL 与检索核对 | “分布先于均值”的推导方向与扇出论证；高延迟片段的常见成因值得专门处理 | 论文讨论的是真实 Web 服务与毫秒级延迟；不能推出本章教学拍数的任何分布形状 |
| 指标必须钉死事件边界，边界不同则数值不可比 | 本项目已验收的 `trustworthy-baseline-chapter` 测量合同与第 07 章终态事件推导（本章显式回指） | 排队/首 token/间隔/端到端由事件对差值定义；换边界换数值的反例可由既有轨迹复算 | 不规定真实系统的事件采集点；那是实现层事实 |
| 分位数需要声明计算方法 | 本章教学定义（最近邻秩） | 小样本分位可手算复核 | 真实系统的分位算法（插值、t-digest 等）不影响定义，只影响实现；不在此展开 |

## 版本锁定的框架实现示例

| 待支持主张 | 一手来源 | 可以支持什么 | 不能推出什么 |
| --- | --- | --- | --- |
| vLLM 压测工具用 `--request-rate` 控制到达：`inf` 表示全部立即到达（压力测试），有限值用 Poisson/Gamma 过程生成到达；`--burstiness` 控制突发程度 | [vllm/docs/benchmarking/cli.md](https://github.com/vllm-project/vllm/blob/main/docs/benchmarking/cli.md)，main@`9e905f7`（2026-09-01），2026-09-02 核对 | 到达过程是负载生成器的显式参数；同一服务在不同到达过程下测出的数字不可互比 | 不提供任何真实速率或延迟数值；不证明某种到达过程“更真实” |
| `--max-concurrency` 限制在途请求数，且客户端并发等待时间作为独立指标 `client_queue_time` 单独报告；探针请求的延迟分位单独报告以测量干扰 | 同上 | 真实工具同样要钉事件边界并把“客户端自己的排队”从服务指标中分离——与本章“换边界换数值”互为印证 | 不声称该工具的指标集完整或其默认值最优 |

SGLang 的压测工具本章不陈述。后续正文若映射更多实现，必须在本表追加：仓库、commit、核对日期与“仅实现示例”边界。

## 必须实测的判断

以下问题在本章只能提出观察需求，不能由教学计数给出结论：

- 任何真实延迟、吞吐、goodput 或 SLO 达成率的数值结论。
- 教学拍数分布与真实负载分布的对应关系（不存在这种对应）。
- 开环与闭环哪种更接近某具体生产流量（需要真实流量到达过程统计）。
- 分位算法（插值、sketch）在大样本下的误差取舍。

真实结论必须沿用第 00 章的测量合同：固定请求集合与生成工作量，记录观察者和完成事件，保留原始事件、失败与环境信息，再限定结论适用范围。

## 确定性教学模型假设

任务组 1 的指标层只用于形成可复核的因果推导：

- 事件底座复用第 07 章 `buildTerminationTrace`（含到达/准入/排队/执行/带原因终态），指标层不另设事件族。
- 固定工作量 `K-a`—`K-f`（6 请求、6 块池、无界排队、prefill 优先），注入两处终止：`K-b` 在 t5 被客户端取消（生成中）、`K-e` 在 t5 超时（首执行期限 t3，排队中）。
- 指标定义为事件对差值：排队 = 准入拍 − 到达拍；首 token = 首个输出事件拍 − 到达拍；间隔 = 相邻输出事件拍之差；端到端 = 终态拍 − 到达拍。教学单位是拍；定义可迁移到真实时间戳，数值不可迁移。
- 分位数用最近邻秩（第 ⌈p·N⌉ 个排序值）并在正文登记；样本量小时 p99 退化为最大值的事实如实呈现。
- 闭环对照：串行客户端（下一请求到达 = 上一请求终态拍 + 1），注入不随环式迁移；开环与闭环共用同一服务与请求集合。
- goodput：有效完成 = 终态原因为 eos/length 的请求；被取消、超时、断开、失败者按原因列入排除清单。
- 均值-尾部反例使用登记的教学分布（A=[1,1,1,1,5]、B=[2,2,2,2,2]，SLO ≤4@p99）；不冒充真实测量。

## 编辑记录

### 2026-09-02 · 事件底座

- 已建立四类来源边界，核对 The Tail at Scale（CACM 56(2) 2013）与 vLLM `docs/benchmarking/cli.md`（main@`9e905f7`：request-rate、burstiness、max-concurrency 与 client_queue_time 单列、探针分位）。
- 本章概念（事件对差值、分布与分位、开放环/闭环、goodput、聚合链）由正文推导命名；实现示例只出现在版本锁定表中。
- 教学数据明确使用 `simulated` 和整数逻辑拍；指标、分位与 SLO 判定全部是证据边界内的记账，禁止字段名或界面暗示真实延迟或性能。
