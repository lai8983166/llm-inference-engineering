# 第 02 章来源与编辑记录

## 使用规则

本章只让“多个请求同时存在”进入系统，所有请求仍继承第 01 章的状态、输出和资源合同。正文中的技术陈述按下列三类管理：稳定原理需要能够追溯到规范或一手文档；运行时与框架事实必须记录版本或访问日期；真实效率判断只能由当前软硬件上的原始测量支持。确定性轨迹属于教学模型，不以引用把假设包装成事实。

## 稳定原理

| 待支持主张 | 一手来源 | 可以支持什么 | 不能推出什么 |
| --- | --- | --- | --- |
| 异步提交允许主机在设备工作完成前继续；实际并发取决于硬件能力、资源和 stream 语义 | [NVIDIA CUDA Programming Guide: Asynchronous Execution](https://docs.nvidia.com/cuda/cuda-programming-guide/02-basics/asynchronous-execution.html)，2026-08-23 核对 | 必须分开主机调用返回、设备开始/完成；多个主机控制流活跃不构成 kernel 同时执行的证据 | 不能推出当前 GPU 上一定重叠、一定串行或吞吐一定提高 |
| 同一 stream 中操作按入队顺序执行，多 stream 的调度仍受可用资源和运行时选择影响 | 同上，CUDA Streams 与 Stream Ordering 小节，2026-08-23 核对 | 设备执行顺序需要 stream、event 与设备轨迹，而不能从线程/协程数量猜测 | 不能把课程中的单设备队列当作 CUDA 的普遍调度实现 |
| 变长序列若要组成规则张量，常见做法是把短序列 padding 到 batch 最长长度或指定长度 | [Hugging Face Transformers: Padding and truncation](https://huggingface.co/docs/transformers/main/en/pad_truncation)，`main` 文档，2026-08-23 核对 | 可以解释第 02 章静态 prefill 案例中“有效 token 与对齐位置”的区别 | 不能推出某个推理 kernel 必然计算全部 padding，也不能量化真实浪费 |
| 每个请求的停止、输出提交与资源释放必须独立裁决 | 本项目已验收的 `single-request-pilot` 主规格及第 01 章状态/所有权推导 | 多请求组织不能抹掉请求身份、终止原因、输出许可和 KV 使用权 | 不规定生产框架的具体类名、锁或生命周期 API |

## 运行时与框架事实

本章底座暂不陈述 vLLM、SGLang 当前 scheduler 的数据结构或默认策略。后续正文若映射真实框架，必须在本表追加：官方文档或源码链接、版本/commit、核对日期，以及它仅用于“实现示例”而不是“概念定义”的边界。不得用博客转述代替能够直接检查的源码或官方设计文档。

CUDA 与 Transformers 的在线文档会随版本演进。正文只采用上表中跨版本稳定的条件性描述；涉及默认 stream、特定 kernel、padding 消除或框架调度行为时必须重新锁定版本，不沿用笼统结论。

## 必须实测的判断

以下问题在本章只能提出观察需求，不能由教学轨迹给出性能结论：

- 两个请求的 kernel 是否重叠、重叠多少，以及 stream 配置是否引入同步。
- 将请求组成 batch 后的 kernel 时间、显存带宽、算力利用率和端到端吞吐如何变化。
- padding、非活跃槽位或重新组织 batch 在具体模型、形状、框架与硬件上的实际成本。
- 某种执行组织对真实 TTFT、后续 token 间隔、尾延迟、失败率和 goodput 的影响。

真实结论必须沿用第 00 章的测量合同：固定请求集合与生成工作量，记录观察者和完成事件，保留原始事件、失败与环境信息，再限定结论适用范围。

## 确定性教学模型假设

任务组 1 的 `StrategyTrace` 只用于形成可复核的因果轨迹：

- 时间使用离散的 `logicalStep`，不对应毫秒、GPU cycle 或等长真实执行区间。
- 串行策略让一个请求完整结束后再选择下一个，用于隔离队头等待。
- 独立循环策略允许各请求分别提交工作，但抽象设备一次只取一个执行组；这是为了显示“主机并发不等于已经证明设备并行”，不是 CUDA 调度模拟器。
- 静态 batch 等待首批两个请求并固定成员直到该批结束，用于暴露 prompt 对齐、提前结束成员和批外等待；cohort 大小与选择规则不是最佳实践。
- 所有策略使用同一份 `RequestFixture`。任何完成顺序差异只能说明这些明确假设下的事件变化。

## 编辑记录

### 2026-08-23 · 事件底座

- 已建立三类来源边界，并核对 CUDA 异步/stream 与变长输入 padding 的官方文档。
- 本阶段尚未撰写方案结论，也没有加入框架版本事实；出现相关内容时必须回到本记录补证据。
- 教学数据明确使用 `simulated` 和逻辑步，禁止字段名或界面暗示真实耗时与 GPU 利用率。

### 2026-08-23 · Prose-only 连续阅读评审

- 评审时正文未挂载动态图、轨迹实践或迁移评估；仅凭固定请求表、串行手算轨迹、主机/设备反例和静态 batch 占用表，已经可以从第二个请求到达连续推到下一次执行选择。
- 五个标题只对应认知任务改变：串行服务暴露等待、独立循环暴露设备语义缺口、静态 batch 暴露形状对齐、成员分化暴露固定生命周期、最后形成未解选择问题。没有另设“概念解释”“工程视角”“优缺点”“小结”等复述标题。
- 串行方案先说明所有权简单和语义正确，再展示 `R-short` 从 step 1 等到 step 4；没有把朴素方案写成等待否定的错误答案。
- padding 与非活跃槽位已分开：前者来自同次执行的输入对齐，后者来自成员生命周期分化；二者均未换算成虚构利用率。
- 删除了按三种方案并列列优缺点的写法，改为让上一方案的具体缺口产生下一方案；章末只形成调度输入，并通过 KV 增长问题进入第 03 章。
- 手算核对结果与 `concurrencyChapterRequests` 一致：串行轨迹 step 0—3 完成 `R-long`、step 4 完成 `R-short`、step 5—6 完成 `R-late`；静态首批的有效 prompt 位置为 8、padding 为 4，`R-short` 在 prefill 后结束，`R-late` 在封闭批次外等待。
- 隐藏所有未来图示与实践后因果链保持完整，因此视觉和实践只能补充状态观察，不能承担正文缺失的结论。

### 2026-08-23 · 图示实现检查（实际视口待验收）

- 请求/设备图只允许在固定 fixture 下切换整请求串行与独立循环；请求数量、到达与逻辑工作保持不变，HOST 和 DEVICE 使用独立轨道与读数。
- 静态 batch 图分别以文字和图案标出有效工作、padding、非活跃槽和批外等待，不依靠颜色作为唯一编码；所有格子来自 `StrategyTrace`，没有另写一份动画数据。
- 两图均支持暂停、前后单步、重置、逻辑步直达和键盘焦点；`prefers-reduced-motion` 下初始暂停，窄屏时工作台内部滚动而不挤压正文。
- 组件、样式、内容、类型和生产构建检查已通过。当前应用内浏览器未提供可用实例，尚未完成桌面/移动实际渲染检查，因此 OpenSpec 任务 3.4、3.5 保持未完成，不把自动化断言冒充视觉验收。

### 2026-08-23 · 图示人工验收

- 用户在本地页面验收了请求/设备轨迹和静态 batch 占用图，并确认继续推进；验收范围沿用交付时列出的策略切换、暂停/单步、逻辑步直达、标题/控制可见与窄屏内部滚动检查项。
- 本记录将用户实际页面验收作为任务 3.4 的人工证据；组件测试、样式测试和生产构建仍单独作为机器证据，不声称应用内浏览器自动化已经恢复。
- 图示保持两种不同构图：第一张是请求、HOST、DEVICE 的因果泳道，第二张是成员随逻辑步变化的占用矩阵；没有复刻第 00 章的测量仪器或第 01 章的单请求脉冲构图。

### 2026-08-23 · 从属推演保留评审

- 轨迹审查保留。两张图负责把既定案例的因果状态变得可观察；这项推演换用未标注的新事件，让学习者必须先区分“尚不可运行”与“已经可运行但未被选择”，再识别有效设备工作和 padding/非活跃占位。它要求主动重建原始记录，认识增量不是复读动画标签。
- 推演被放在五段完整正文之后。移除整个组件时，从第二个请求到达、朴素串行、独立控制流、静态 batch 到下一次执行选择的正文推导仍然成立，因此实践不承担概念首讲或关键结论。
- 每条归类结果均标记为 `simulated · 非真实 GPU 性能证据`，汇总再明确禁止据此声称执行重叠、利用率或性能收益。迁移评估另换 Q-A/Q-B/Q-C 案例，只检查事件重建、设备因果、请求不变量、证据边界与下一次选择输入，不要求记忆方案名称。
- 交互状态只存在组件内存中；预测、检查、自由文本和展开标准均不写入本地存储，也不形成学习进度或掌握状态。

### 2026-08-23 · 完整章节人工验收

- 用户沿最终路由验收了第 02 章，并在新增事件归类与迁移审查交付后确认继续推进。验收范围包括固定案例从正文到图示的连续性、五个正文标题的密度与概念出现时机、正文—图示—从属推演—迁移审查的顺序，以及桌面和窄屏下的交互可见性。
- 最终页面保持同一组 `R-long`、`R-short`、`R-late` 请求贯穿三种朴素组织；练习与评估另换事件，不倒置正文的概念形成过程，也没有增加重复方案小结或进度面板。
- 图示在 reduced-motion 偏好下停止自动推进，长时间线和原始事件表在窄屏内部滚动；人工验收未提出新的遮挡、裁切或阅读断裂问题，因此本轮不追加视觉修正。
- 本记录把用户实际阅读验收作为人工证据；DOM、样式、可访问性、reduced-motion 声明和构建结果仍由自动化检查单独支持，二者不互相冒充。

### 2026-08-23 · 最终自动化校验

- `npm run check` 完整通过：ESLint 无错误，14 个测试文件的 63 项测试全部通过，三章内容结构有效，TypeScript 构建和 Vite 生产构建成功。
- `openspec validate build-naive-concurrency-chapter --strict` 通过。确定性逻辑步仍只用于事件先后、等待来源和占位分类，没有被换算成真实耗时、GPU 利用率或框架性能结论。
