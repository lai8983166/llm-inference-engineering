# 第 09 章来源与编辑记录

## 使用规则

本章把优化工作变成证据链：预算构成、瓶颈指纹、收益上限与噪声对照。正文中的技术陈述按四类管理：稳定算术与归因原理需要能够追溯到一手文献；框架实现示例必须记录仓库、版本/commit 与核对日期，并只用于“实现示例”而不是“概念定义”；真实耗时、带宽与收益数值只能由对应软硬件上的测量支持；确定性教学模型假设只用整数预算单位与固定记录样本，不以引用把假设包装成事实。

## 稳定算术与归因原理

| 待支持主张 | 一手来源 | 可以支持什么 | 不能推出什么 |
| --- | --- | --- | --- |
| 部分优化的收益上限由该部分占比决定：上限 = 1/(1−占比)（部件趋零时的极限） | [Amdahl, Validity of the Single Processor Approach, AFIPS 1967](https://dl.acm.org/doi/10.1145/1465482.1465560)，2026-09-03 经 ACM DL 核对 | “先查占比再动手”的算术依据；上限不可被手段超越 | 原文讨论并行处理器；本章将其用于任何部分优化的上限推理，属一般化引用，不冒充原文语境 |
| 端到端成本为部件之和时，局部收益按占比稀释 | 本章第 1 节预算表推导（延续第 00 章测量合同） | “kernel 快一倍而服务快 5%”的算术可手算复核 | 串行求和是登记的教学简化；真实系统的重叠只会让局部收益更难兑现，不建模 |
| 观察会改变被观察系统（观测者效应） | 本项目已验收的 `trustworthy-baseline-chapter` 测量合同与下方版本锁定示例 | profiler 结果含有 profiler 自身的开销贡献，须在结论中登记 | 不量化任何真实 profiler 的开销数值 |

## 版本锁定的框架实现示例

| 待支持主张 | 一手来源 | 可以支持什么 | 不能推出什么 |
| --- | --- | --- | --- |
| vLLM 提供开发者级 profiling：`--profiler-config`/`vllm bench serve --profile`；文档明确“profiling 会显著拖慢推理，终端用户不应开启”，并建议性能关键分析用低开销的 Nsight Systems、富信息分析用 PyTorch Profiler 但其特性“会增加开销，不建议用于 benchmarking” | [vllm/docs/contributing/profiling.md](https://github.com/vllm-project/vllm/blob/main/docs/contributing/profiling.md)，main@`a367cdb`（2026-08-11），2026-09-03 核对 | 真实框架存在 profiler 取舍（低开销 vs 富信息）与观测者效应；工具选择本身就是实验设计的一部分 | 不提供任何真实开销数值；不比较两款工具的优劣结论；trace 分析方法不在本章范围 |

NVIDIA Nsight 与 PyTorch Profiler 的具体使用方法本章不教授。后续正文若映射更多实现，必须在本表追加：仓库、commit、核对日期与“仅实现示例”边界。

## 必须实测的判断

以下问题在本章只能提出观察需求，不能由预算单位或固定样本给出结论：

- 任何真实服务的部件占比（需现场 profiler 测量，且登记 profiler 自身开销）。
- 任何优化的真实收益与上限逼近程度（需最小对照实验按分布比较）。
- 真实噪声幅度与样本量需求（教学样本表是固定记录，不是真实分布）。
- CUDA Graph、量化、算子融合等机制在具体模型与硬件上的真实代价。

真实结论必须沿用第 00 章的测量合同：固定请求集合与生成工作量，记录观察者和完成事件，保留原始事件、失败与环境信息，再限定结论适用范围。

## 确定性教学模型假设

任务组 1 的预算领域模型只用于形成可复核的算术：

- 一个解码步的成本分解为五部件整数单位：提交（submit）2、launch 间隙（launch）8、同步（sync）2、访存（memory）6、计算（compute）2，合计 20 单位；launch 主导使反例鲜明。
- 优化是部件的比例缩放：更快 kernel（compute ×0.5）、CUDA Graph 一类机制（launch ×0.25）、KV 量化（memory ×0.5）、去同步（sync ×0）。
- 收益 = 优化前合计 / 优化后合计；上限 = 部件归零时的 1/(1−占比)。
- 噪声样本是两组固定记录的教学样本（无实质变化：前 [18,19,20,21,22]、后 [19,20,20,20,21]；真实收益：launch 优化后 [13,14,14,15,15]），不是随机生成，也不代表真实噪声分布。
- 预算单位与样本读数不对应任何真实时间量纲；字段名与界面禁止出现毫秒、GB/s、FLOPS 或利用率。

## 编辑记录

### 2026-09-03 · 事件底座

- 已建立四类来源边界，核对 Amdahl 1967（ACM DL）与 vLLM `docs/contributing/profiling.md`（main@`a367cdb`：观测者效应警告、Nsight/PyTorch 取舍、--profile 旗标）。
- 本章概念（预算表、指纹、上限、噪声判定、归因链）由正文推导命名；实现示例只出现在版本锁定表中。
- 教学数据明确使用 `simulated` 预算单位与固定样本；禁止字段名或界面暗示真实耗时、带宽或收益。
