# 第 07 章来源与编辑记录

## 使用规则

本章把取消、超时、断开与失败纳入状态机，建立终态统一与无泄漏合同。正文中的技术陈述按四类管理：稳定终止与清理原理需要能够追溯到论文、一手文档或本项目已验收推导；框架实现示例必须记录仓库、版本/commit 与核对日期，并只用于“实现示例”而不是“概念定义”；真实连接检测、故障注入与清理耗时只能由对应软硬件上的测量支持；确定性教学模型假设只用整数逻辑拍与计数，不以引用把假设包装成事实。

## 稳定终止与清理原理

| 待支持主张 | 一手来源 | 可以支持什么 | 不能推出什么 |
| --- | --- | --- | --- |
| 每个请求的停止、输出提交与资源释放必须独立裁决；终止时要等在途工作安全结束再释放 KV | 本项目已验收的 `single-request-pilot` 主规格与第 01 章状态/所有权推导（本章显式回指） | 终止是请求级合同；部分输出语义（已送达的 token 保持送达）来自第 01 章输出许可推导 | 不规定多请求下的取消传播与容量回收——那是本章与第 06 章的推导 |
| 释放只能发生在在途读取安全结束之后（第 03 章在途合同） | 本项目已验收的 `kv-state-chapter` 规格 | 清理仪式中“在途安全”步骤的依据 | 不给出真实系统中在途检测的实现方式 |
| vLLM 将 abort 实现为一等终态：`abort_requests` 经调度器 `finish_requests(request_ids, RequestStatus.FINISHED_ABORTED)` 统一收尾；代码注释说明当前未区分“客户端放弃”与“自然停止”的完成原因（TBD） | [vllm/v1/engine/core.py](https://github.com/vllm-project/vllm/blob/main/vllm/v1/engine/core.py)，main@`17da485`（2026-08-26），2026-08-28 核对 | 真实框架确实把多种终点汇入同一条调度器收尾路径——与本章“终态统一”推导互为印证；`output_processor.py`（main@`903a021`）提供 `abort_requests` 接口 | 不提供清理耗时、连接检测方式或失败传播半径；其注释中的工程取舍（合并原因）不是本章必须效仿的规范 |
| 引擎执行失败有专门的错误处理路径（`log_error_detail` 上下文在 `execute_model` 异常时记录细节） | 同上 `core.py`（main@`17da485`） | 真实系统区分执行错误路径与正常完成路径 | 失败的爆炸半径（哪些请求随之终止）取决于实现与部署，本章不引用其行为作结论 |

## 版本锁定的框架实现示例

| 待支持主张 | 一手来源 | 可以支持什么 | 不能推出什么 |
| --- | --- | --- | --- |
| abort 由外部请求 ID 或内部 ID 触发，经 `abort_requests` 进入输出处理与调度器 | [vllm/v1/engine/output_processor.py](https://github.com/vllm-project/vllm/blob/main/vllm/v1/engine/output_processor.py)，main@`903a021`（2026-08-26），2026-08-28 核对 | 显式取消在真实引擎中是贯穿输出处理与调度的显式路径 | 不规定 HTTP 层断开检测、超时参数或重试语义 |

SGLang 的取消与断开处理本章不陈述。后续正文若映射更多实现，必须在本表追加：仓库、commit、核对日期与“仅实现示例”边界。

## 必须实测的判断

以下问题在本章只能提出观察需求，不能由教学计数给出结论：

- 真实系统中连接断开的检测延迟与其间的无效计算量。
- 清理路径（释放块、离队、关流）在真实引擎中的耗时与尾延迟影响。
- 故障注入下请求级与引擎级失败的真实发生率与爆炸半径。
- 终止语义对 goodput 的真实影响（无效 hope 占用的资源量需要观测）。

真实结论必须沿用第 00 章的测量合同：固定请求集合与生成工作量，记录观察者和完成事件，保留原始事件、失败与环境信息，再限定结论适用范围。

## 确定性教学模型假设

任务组 1 的终止领域模型只用于形成可复核的因果轨迹：

- 池 6 块 × 4 unit、无界排队策略、prefill 优先与拍内顺序全部延续第 05/06 章；终止在拍内最先处理（先于到达与准入重试）。
- 工作量：`C-a`/`C-b`（t0，5+3）、`C-c`（t1，5+3）、`C-d`（t2，3+2）、`C-e`（t3，2+1）。
- 终止注入两处：`C-b` 在 t4 被客户端取消（生成中，持有 2 块）；`C-e` 首执行期限 t3，t4 仍未执行则超时离队。
- 终态原因六类：eos、length（自然，来自 fixture）、client-cancel、timeout、disconnect、error（注入）。注入按请求级作用；引擎级整组失败只在正文与评估中分级讨论，不进主轨迹。
- 清理仪式固定顺序：终止裁决 → 停止增长 → 释放块（如有）→ 离队（如在队）→ 关闭流 → 终态事件；释放块沿用第 03 章“在途读取安全后”的约定，教学模型中体现为终态当拍完成全部清理。
- 无泄漏不变量由 `noLeakIssues` 逐事件校验：每请求恰一终态；终态后无增长/执行/排队事件；终态当拍块归零；在队者离队；流关闭事件紧随终态。
- 拍数与计数不换算为延迟、可靠性或资源回收收益。

## 编辑记录

### 2026-08-28 · 事件底座

- 已建立四类来源边界，直接核对 vLLM `v1/engine/core.py`（main@`17da485`，abort 终态与错误路径）与 `v1/engine/output_processor.py`（main@`903a021`，abort 接口）；其“合并完成原因”的注释登记为工程取舍而非规范。
- 本章概念（终态统一、清理仪式、无泄漏合同、爆炸半径）由正文推导命名；实现示例只出现在版本锁定表中。
- 教学数据明确使用 `simulated` 和整数逻辑拍/计数，禁止字段名或界面暗示真实耗时、延迟或可靠性结论。

### 2026-08-28 · Prose-only 连续正文评审

- 评审时未挂载任何动态图、终止实践或迁移评估；仅凭状态×事件组合、清理仪式表、双注入对照表、爆炸半径分级与无泄漏合同，已经可以从“无效希望”连续推到“可观测的终点”与第 08 章入口。
- 五个标题只对应认知任务变化：取消落在任何状态、终态统一、隐式来源、爆炸半径、无泄漏合同。没有另设“概念解释”“工程视角”“优缺点”“小结”等复述标题。
- 取消先写成状态×事件的组合（排队/待 prefill/生成/重算恢复四种转移），**清理仪式**在组合摊开之后才出现；仪式表七步逐一标注前章依据（第 01 章输出许可、第 03 章在途安全、第 04 章整块归还、第 05 章成员重算）。
- 双注入对照逐格与 `buildTerminationTrace` 一致：注入 6 拍对基线 8 拍；`C-b` 取消当拍释放 2 块、`C-d` 同拍准入首执行 t4（基线 t5）早两拍完成；`C-a`/`C-c` 完成拍 t4→t5（清理的调度后果如实呈现）；`C-e` 超时离队零块。正文明确“检测是边缘的事，转移是状态机的事”。
- 部分输出合同回指第 01 章（已送达的 token 保持送达）；请求级与引擎级失败各给小算例，明确“半径决定谁终止，不决定怎样终止”，不评判哪种半径正确。
- 无泄漏合同以 `noLeakIssues` 落地：正文描述的三条不变量与校验器逐项对应；终态事件被定位为第 08 章指标的证据源（“指标是对可观测终点的聚合”）。
- 隐藏所有未来图示与实践后因果链保持完整；全文未出现毫秒、GB/s、利用率、TTFT、ITL，“延迟/可靠性/收益”字样均指向测量边界。

### 2026-08-28 · 图示实现检查（实际视口待验收）

- “六条终点，同一次清理”图以生命周期泳道加终态原因色带呈现注入轨迹；终态当拍显示“零块✓ / 离队✓ / 关流✓”清理对勾（离队对勾仅对终态时在队的请求出现），逐拍推进可核对清理紧随终态。
- “终止也是容量回收”图将基线与注入终止的空闲块条带逐拍并排，读数给出每请求“基线 → 注入”的首执行/终态对照与总拍数 8→6，`C-d` 提前一拍准入与幸存者晚一拍完成并列呈现。
- 两图数据均直接消费 `buildTerminationTrace` 与 `terminationOutcome`，没有第二套动画数字；组件测试核对双终态拍的对勾组合与对照行。
- 两图支持暂停、前后单步、拍直达、重置、键盘与 reduced-motion 初始暂停；泳道与条带窄屏内部横向滚动；图注明确对勾与拍数是记账而非延迟或可靠性。
- 组件、样式、内容、类型与生产构建检查通过。桌面与移动视口的实际渲染检查尚未进行，OpenSpec 任务 3.4、3.5 保持未完成，不以自动化断言冒充视觉验收。
