## 1. 建立第 04 章载体与块池底座

- [x] 1.1 新建第 04 章来源与编辑记录，按“稳定分页/容量原理、版本锁定的框架实现示例、必须实测的硬件判断、确定性教学假设”分类；核对操作系统分页原理、PagedAttention 论文与 vLLM/SGLang 块实现文档，逐项记录能支持和不能推出的结论。
- [x] 1.2 实现 `BlockPoolFixture`、`BlockLease`、`BlockTableSnapshot`、`BlockPoolEvent` 与 `BlockPoolSnapshot` 数据边界，以及块池轨迹生成、任意块大小的表项/浪费计数、与第 03 章连续轨迹对照的纯函数；用单元测试验证固定池为 6 块 × 4 unit、三请求工作量不变、完成态 3/1/2 块与 2/1/2 unit 内部浪费、块内追加与整块释放顺序合法，且数据不含真实耗时或 GPU 利用率字段。
- [x] 1.3 新建第 04 章 MDX、页面载体和稳定路由，接入首页入口并为第 03 章补下一章导航；用路由测试验证 00→01→02→03→04 均可直达、第 04 章能返回第 03 章且不会生成尚不存在的第 05 章链接。
- [x] 1.4 运行领域模型、路由、内容与类型检查，确认本组只建立载体、来源记录和确定性块池底座后提交独立 Git commit；用 `git show --stat` 核对没有纳入既有 `package-lock.json` 改动。

## 2. 完成从布局合同到块大小交换的正文

- [x] 2.1 从第 03 章四条合同开始，先简要收束压缩为何只是修补，再检查“拆成多段”是否逐条保住合同，让迁移从被迫变为不存在、映射成为新问题；人工核对正文没有以任何框架数据结构作为评估标准。
- [x] 2.2 用手算表展示变长分段的空位匹配失败，推导等长块取消匹配问题、把代价转为每请求封顶的内部浪费；用内容测试锁定“块”的命名晚于匹配矛盾。
- [x] 2.3 建立块表合同：逻辑位置到表项加偏移的翻译、追加的分配-登记-写入顺序、错表项的正确性后果，并把表元数据写入可计数账本；用固定算例验证表项数与逻辑长度的关系可复算。
- [x] 2.4 沿三请求逻辑步写出块池轨迹：块内追加不分配、`R-short` 归还块被 `R-long` 复用、取消与完成只整块归还自己的块；核对每个块编号、表项与浪费 unit 均回到同一 fixture。
- [x] 2.5 与第 03 章连续按需轨迹对照迁移事件、拒绝与最终状态，重放 step 1 末碎片场景得到第三种结局；对照说明块池消除的是连续性约束而非内部浪费或元数据。
- [x] 2.6 用 B=1、2、4、8、24 手算表项数与内部浪费的反向关系，说明两个极端退化为逐 token 映射与单一连续区间；真实查找与 kernel 成本只登记为测量问题，不给出推荐块大小。
- [x] 2.7 隐藏所有图示与实践执行 prose-only 编辑评审，删除公式卡堆、并列优缺点、重复小结和脱离因果链的工程附录，确认五个标题只对应认知任务改变；将修正写入编辑记录，运行正文结构、固定算例、术语顺序、内容与类型检查后提交独立 Git commit。

## 3. 让块表翻译与块池演化可观察

- [x] 3.1 实现“块表怎样翻译一次读取”图：沿一次 decode 展示逻辑位置带 → 块表 → 散布物理块的翻译路径，允许在连续区间直读与块表翻译间切换；用组件测试验证两条路径读取同一批逻辑位置、差异只在翻译步骤，且界面不声称速度差异。
- [x] 3.2 实现“同一池的第三种结局”图：以块对齐地址带与事件账本展示块池轨迹，允许切换到第 03 章连续按需轨迹；用单元与组件测试核对每个块状态、空闲块数、内部浪费与迁移事件计数均来自领域轨迹。
- [x] 3.3 将两图分别放在映射合同与池对照形成之后，补齐暂停、前后单步、重置、键盘、当前事件、原始块编号、文字/图案编码、reduced-motion 与窄屏内部滚动；通过可访问性和样式测试验证控制不遮标题、颜色不是唯一编码且块编号不被标成真实显存地址。
- [ ] 3.4 在桌面与移动视口实际检查两张图的完整过程，确认逻辑位置带、表项、块边界、复用与释放状态不被裁切，隐藏任一图也不破坏正文结论；把发现、修正和人工证据写入第 04 章编辑记录。
- [ ] 3.5 运行图示、领域模型、样式、内容、类型与生产构建检查，确认两图分别只回答映射翻译和池演化且未复制前章构图后提交独立 Git commit；用 `git show --stat` 核对提交范围。

## 4. 决定从属推演并完成迁移验收

- [ ] 4.1 在完整正文之后实现候选块池事件审查，先让学习者预测首个失败或首个复用事件说明什么，再重建一次合法的分配-登记-追加-整块释放顺序或一次逻辑位置翻译；用组件测试验证预测先于解释、原始事件与块状态始终可见且每个结果标为模拟证据。
- [ ] 4.2 对照两张解释图评审候选实践的认识增量：只有它要求主动组合映射、粒度与所有权而非复读图示时才保留，否则删除；把决定理由以及移除实践后正文仍完整的结论写入编辑记录。
- [ ] 4.3 实现不同于正文的迁移评估，换用块大小、模型维度、请求长度与池规模，依次检查一个致命账本或映射错误、浪费归因、翻译与回收顺序、证据边界与待解问题；用测试验证结构化首问和展开标准覆盖六个维度且不以背诵术语为完成证据。
- [ ] 4.4 将保留的实践和迁移评估放在完整正文之后，确认访问页面、播放图示、展开答案或填写自由文本不会生成掌握状态；通过 DOM、存储写入和人工文本检查验证所有块编号/步骤旁均说明不能证明真实显存行为或性能。
- [ ] 4.5 运行实践、评估、内容、类型和生产构建检查，确认本组只增加主动重建与迁移诊断证据后提交独立 Git commit；用 `git show --stat` 核对提交范围。

## 5. 完成第 04 章交付与验收

- [ ] 5.1 更新 README 当前内容和 00→01→02→03→04 阅读顺序，完成首页与第 03、04 章前后导航；用链接检查和路由测试验证已发布入口全部有效且未链接未实现的第 05 章。
- [ ] 5.2 在桌面与移动视口完整阅读第 04 章，逐项检查固定模型/请求/块参数连续性、标题密度、手算可复算性、概念命名时机、图文顺序、交互状态、窄屏可见性和 reduced-motion；在编辑记录中记录发现、修正与最终判断。
- [ ] 5.3 运行 `npm run check` 和 `openspec validate build-paged-kv-chapter --strict`，确认第 00—04 章均通过内容、类型、测试与生产构建，且块编号、表项与逻辑步始终保持证据边界。
- [ ] 5.4 更新任务完成状态并提交最终交付 Git commit，用 `git log` 与 `git status --short` 确认各实施组均有独立提交且没有误纳入既有 `package-lock.json` 改动，然后交由用户体验验收。
