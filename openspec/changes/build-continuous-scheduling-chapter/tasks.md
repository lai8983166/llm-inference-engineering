## 1. 建立第 05 章载体与调度底座

- [x] 1.1 新建第 05 章来源与编辑记录，按“稳定调度原理、版本锁定的框架实现示例、必须实测的判断、确定性教学假设”分类；核对 Orca 论文的 iteration-level scheduling 与 vLLM 引擎循环文档，逐项记录能支持和不能推出的结论。
- [x] 1.2 实现 `SchedulePolicy`、`ScheduleGroup`、`ScheduleEvent` 与 `ScheduleTickSnapshot` 数据边界，以及双策略轨迹生成、封闭批次对照纯函数与轨迹校验器；用单元测试验证每拍至多一份工作、prefill 优先 6 拍且首执行拍 0/1/3、decode 优先 7 拍且首执行拍 0/4/5、准入不超 6 块池、完成恰一次，且数据不含真实耗时或 GPU 利用率字段。
- [x] 1.3 新建第 05 章 MDX、页面载体和稳定路由，接入首页入口并为第 04 章补下一章导航；用路由测试验证 00→01→02→03→04→05 均可直达、第 05 章能返回第 04 章且不会生成尚不存在的第 06 章链接。
- [x] 1.4 运行领域模型、路由、内容与类型检查，确认本组只建立载体、来源记录和确定性调度底座后提交独立 Git commit；用 `git show --stat` 核对没有纳入既有 `package-lock.json` 改动。

## 2. 完成从决定点到策略合同的正文

- [x] 2.1 把第 02 章的遗留问题形式化为逐拍决定点，说明静态 batch 是沿用上次答案的特例；人工核对正文没有以任何框架机制作为出发点。
- [x] 2.2 用六项输入逐条定义可运行集合，为不可运行分原因（未到达、待 prefill、等待块、已完成），并说明集合每拍重算；用内容测试锁定原因区分先于策略讨论。
- [x] 2.3 沿三请求逻辑步写出双策略逐拍轨迹：晚到者当拍加入、完成者当拍离开、decode 组等宽无 padding；对照第 02 章封闭批次的首执行拍差异；“连续批处理”命名晚于行为推导。
- [x] 2.4 手算并列 prefill 优先与 decode 优先的完整时间线（首执行拍与完成拍），说明竞争使等待在晚到者与在跑者之间迁移；核对每个拍数、块数与首执行拍均回到同一 fixture，且不把拍数写成延迟。
- [x] 2.5 以合同清单收束（拍节奏、可运行定义、选择规则、准入上限），登记容量压力、抢占与公平性为未解问题；Orca 与 vLLM 引用只作推导印证与实现示例，不写成最佳实践。
- [x] 2.6 隐藏所有图示与实践执行 prose-only 编辑评审，删除公式卡堆、并列优缺点、重复小结和脱离因果链的工程附录，确认五个标题只对应认知任务改变；将修正写入编辑记录，运行正文结构、固定算例、术语顺序、内容与类型检查后提交独立 Git commit。

## 3. 让集合重组与时间线可观察

- [x] 3.1 实现“可运行集合怎样每拍重组”图：逐拍账本展示到达/准入行、请求状态芯片、被选执行与等待队列，策略可切换；用组件测试验证状态与选择均来自轨迹、等待块与未被选中的证据可区分。
- [x] 3.2 实现“同一份工作量的三种时间线”图：请求生命周期泳道沿拍轴并列封闭批次、prefill 优先、decode 优先；用单元与组件测试核对首执行拍、完成拍与总拍数均来自领域轨迹。
- [x] 3.3 将两图分别放在成员重组与策略对照形成之后，补齐暂停、前后单步、重置、键盘、当前拍、原始拍数、文字/图案编码、reduced-motion 与窄屏内部滚动；通过可访问性和样式测试验证控制不遮标题、颜色不是唯一编码且拍数不被标成时间。
- [ ] 3.4 在桌面与移动视口实际检查两张图的完整过程，确认状态芯片、等待标注、泳道与完成标记不被裁切，隐藏任一图也不破坏正文结论；把发现、修正和人工证据写入第 05 章编辑记录。
- [ ] 3.5 运行图示、领域模型、样式、内容、类型与生产构建检查，确认两图分别只回答集合重组和时间线对照且未复制前章构图后提交独立 Git commit；用 `git show --stat` 核对提交范围。

## 4. 决定从属推演并完成迁移验收

- [x] 4.1 在完整正文之后实现候选逐拍账本审查（新工作量与新池，准入至少一次真正等待块），先让学习者判断首个等待的原因，再重建一拍的合法顺序（到达检查→准入→可运行集合→选择→执行→完成释放）；用组件测试验证预测先于解释、原始事件全程可见且每个结果标为模拟证据。
- [x] 4.2 对照两张解释图评审候选实践的认识增量：只有它要求主动组合决定点、可运行合同与竞争权衡而非复读图示时才保留，否则删除；把决定理由以及移除实践后正文仍完整的结论写入编辑记录。
- [x] 4.3 实现不同于正文的迁移评估，换用策略、请求长度与更小的池，依次检查一个致命理解错误（等待块当调度选择或拍数当延迟）、原因归类、首执行拍复算、证据边界与待解问题；用测试验证结构化首问和展开标准覆盖六个维度且不以背诵术语为完成证据。
- [x] 4.4 将保留的实践和迁移评估放在完整正文之后，确认访问页面、播放图示、展开答案或填写自由文本不会生成掌握状态；通过 DOM、存储写入和人工文本检查验证所有拍数/状态旁均说明不能证明真实延迟或性能。
- [x] 4.5 运行实践、评估、内容、类型和生产构建检查，确认本组只增加主动重建与迁移诊断证据后提交独立 Git commit；用 `git show --stat` 核对提交范围。

## 5. 完成第 05 章交付与验收

- [ ] 5.1 更新 README 当前内容和 00→01→02→03→04→05 阅读顺序，完成首页与第 04、05 章前后导航；用链接检查和路由测试验证已发布入口全部有效且未链接未实现的第 06 章。
- [ ] 5.2 在桌面与移动视口完整阅读第 05 章，逐项检查固定模型/请求/块参数连续性、标题密度、手算可复算性、概念命名时机、图文顺序、交互状态、窄屏可见性和 reduced-motion；在编辑记录中记录发现、修正与最终判断。
- [ ] 5.3 运行 `npm run check` 和 `openspec validate build-continuous-scheduling-chapter --strict`，确认第 00—05 章均通过内容、类型、测试与生产构建，且拍数、状态与等待始终是证据边界内的计数。
- [ ] 5.4 更新任务完成状态并提交最终交付 Git commit，用 `git log` 与 `git status --short` 确认各实施组均有独立提交且没有误纳入既有 `package-lock.json` 改动，然后交由用户体验验收并归档。
