## 1. 建立第 03 章载体与 KV 状态底座

- [x] 1.1 新建第 03 章来源与编辑记录，按“稳定张量/容量原理、模型或框架版本事实、必须实测的硬件判断、确定性教学假设”分类；核对 Attention、GQA/MQA、模型配置字段与 KV 布局相关一手来源，并逐项记录能支持和不能推出的结论。
- [x] 1.2 实现 `KvModelFixture`、`KvRequestFixture`、`KvAllocation`、`KvRequestSnapshot`、`KvMemoryEvent` 与 `KvPoolSnapshot` 数据边界，以及 KV 字节计算、最大预留轨迹、按需连续增长轨迹和容量失败分类纯函数；用单元测试验证固定 fixture 为 128 bytes/token、三请求工作量不变、区间不重叠、地址发布/释放顺序合法且数据不含真实耗时或 GPU 利用率字段。
- [x] 1.3 新建第 03 章 MDX、页面载体和稳定路由，接入首页入口并为第 02 章补下一章导航；用路由测试验证 00→01→02→03 均可直达、第 03 章能返回第 02 章且不会生成尚不存在的第 04 章链接。
- [x] 1.4 运行领域模型、路由、内容与类型检查，确认本组只建立载体、来源记录和确定性状态底座后提交独立 Git commit；用 `git show --stat` 核对没有纳入既有 `package-lock.json` 改动。

## 2. 完成从历史依赖到连续布局失败的正文

- [x] 2.1 从第 01 章同一请求的下一次 decode 写出无缓存重算路径，让当前 query 对历史 K/V 的依赖先于 KV cache 术语出现；人工核对正文区分跨步 K/V 与本步中间量，并说明位置、模型层、上下文与数值表示属于复用正确性合同。
- [x] 2.2 用固定 4 层、8 query heads、2 KV heads、head dimension 4、2 bytes/element 模型逐项推导 128 bytes/token，再计算 `R-long`、`R-short`、`R-late` 的完成态有效 KV；用内容测试和独立手算验证公式使用 KV heads、保留原始维度且未混入权重、激活、工作区或元数据。
- [x] 2.3 沿三请求逻辑步写出 prefill 建立历史、decode 每步追加、提前结束与在途读取后的独立释放，使 KV 从字节公式变成有所有权的请求状态；核对每个长度、字节、权威地址和释放许可均能回到同一 fixture。
- [x] 2.4 先分析按最大长度一次预留为何能稳定地址、简化边界并避免搬迁，再让请求上限与实际长度差异造成准入压力；用 24 token-unit 教学池手算区分有效 KV、保留未用和剩余容量，不把方案写成稻草人。
- [x] 2.5 从减少预留的动机推导按需连续增长，依次说明原地扩展、另址申请、双份存活、复制完成、地址发布、等待在途读取和旧区间释放；用事件表核对任一步失败时旧地址仍是权威状态，取消也不会提前回收。
- [x] 2.6 用同一物理池中请求独立结束产生的孔洞，区分有效容量耗尽、过度预留、搬迁峰值和外部碎片；正文以“逻辑 token 连续是否要求单一连续物理区间”收束，并通过术语检查确认没有提前教授 block、page、block table 或 paged attention。
- [x] 2.7 隐藏所有图示与实践执行 prose-only 编辑评审，删除公式卡堆、并列优缺点、重复小结和脱离因果链的工程附录，确认五个标题只对应认知任务改变；将修正写入编辑记录，运行正文结构、固定算例、术语顺序、内容与类型检查后提交独立 Git commit。

## 3. 让历史复用与物理区间演化可观察

- [x] 3.1 实现“历史为何仍被读取”图，在同一 decode 步切换重算历史与复用历史，逐步显示当前 query、历史 K/V 读取、当前位置 K/V 追加和本步中间量消费；用组件测试验证两条路径保持 token 历史与语义边界一致，且界面不声称虚构的速度收益。
- [x] 3.2 实现“连续区间怎样被逼到搬迁”图，在同一 24-unit 池和三请求工作量下切换最大预留与按需连续增长，逐步显示已用/预留、空闲孔洞、迁移双份存活、地址发布、在途读取和释放；用单元与组件测试核对每个区间、总空闲和最大连续空闲均来自领域轨迹。
- [x] 3.3 将两图分别放在对应矛盾形成之后，补齐暂停、前后单步、重置、键盘、当前事件、原始数值、文字/图案编码、reduced-motion 与窄屏内部滚动；通过可访问性和样式测试验证控制不遮标题、颜色不是唯一编码且逻辑地址不会被标成真实 GPU 地址。
- [ ] 3.4 在桌面与移动视口实际检查两张图的完整过程，确认历史 token、K/V 标签、地址刻度、双份存活与释放状态不被裁切，隐藏任一图也不破坏正文结论；把发现、修正和人工证据写入第 03 章编辑记录。
- [ ] 3.5 运行图示、领域模型、样式、内容、类型与生产构建检查，确认两图分别只回答依赖复用和物理演化且未复制前章构图后提交独立 Git commit；用 `git show --stat` 核对提交范围。

## 4. 决定从属推演并完成迁移验收

- [x] 4.1 在完整正文之后实现候选内存事件审查，先让学习者预测首个失败属于有效容量、过度预留、迁移峰值或外部碎片，再要求重建一次地址发布与旧区间释放顺序；用组件测试验证预测先于解释、原始区间始终可见且每个结果标为模拟证据。
- [x] 4.2 对照两张解释图评审候选实践的认识增量：只有它要求主动组合字节账本、连续区间和在途所有权而非复读图示时才保留，否则删除；把决定理由以及移除实践后正文仍完整的结论写入编辑记录。
- [x] 4.3 实现不同于正文的迁移评估，换用模型层数、KV heads、dtype、请求长度和物理区间，依次检查首个致命账本/所有权错误、容量归因、搬迁释放、证据边界与待解布局合同；用测试验证结构化首问和展开标准覆盖六个维度且不询问分页方案名称。
- [x] 4.4 将保留的实践和迁移评估放在完整正文之后，确认访问页面、播放图示、展开答案或填写自由文本不会生成掌握状态；通过 DOM、存储写入和人工文本检查验证所有逻辑地址/步骤旁均说明不能证明真实 GPU 显存行为或性能。
- [x] 4.5 运行实践、评估、内容、类型和生产构建检查，确认本组只增加主动重建与迁移诊断证据后提交独立 Git commit；用 `git show --stat` 核对提交范围。

## 5. 完成第 03 章交付与验收

- [ ] 5.1 更新 README 当前内容和 00→01→02→03 阅读顺序，完成首页与第 02、03 章前后导航；用链接检查和路由测试验证已发布入口全部有效且未链接未实现的第 04 章。
- [ ] 5.2 在桌面与移动视口完整阅读第 03 章，逐项检查固定模型/请求连续性、标题密度、公式可复算性、概念命名时机、图文顺序、交互状态、窄屏可见性和 reduced-motion；在编辑记录中记录发现、修正与最终判断。
- [ ] 5.3 运行 `npm run check` 和 `openspec validate build-kv-state-chapter --strict`，确认第 00—03 章均通过内容、类型、测试与生产构建，且模拟地址、容量和逻辑步始终保持证据边界。
- [ ] 5.4 更新任务完成状态并提交最终交付 Git commit，用 `git log` 与 `git status --short` 确认各实施组均有独立提交且没有误纳入既有 `package-lock.json` 改动，然后交由用户体验验收。
