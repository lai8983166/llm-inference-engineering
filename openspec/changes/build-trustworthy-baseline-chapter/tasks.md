## 1. 建立章节载体与证据底座

- [x] 1.1 新建第 00 章来源与编辑记录，按“稳定原理、工具/框架事实、必须实测的判断”标注正文依据，并通过人工核对确认每个关键技术主张都有一手来源或明确的推导边界。
- [x] 1.2 实现最小 `BenchmarkEvent`、`BenchmarkRun`、`MeasurementWindow` 与协议数据边界，以及从原始事件计算窗口和拒绝无效记录的纯函数；用单元测试验证正常、缺失事件、事件逆序和不可比工作量。
- [x] 1.3 新建第 00 章 MDX 与独立页面外壳，接入稳定路由并让首页按 00、01 顺序提供入口，同时保持 `/chapters/single-request` 可直达；用路由测试和内容校验验证两个章节均可加载。
- [x] 1.4 运行本组相关单元测试、`npm run check:content` 与类型检查，确认只包含第 00 章载体和证据底座后提交一个独立 Git commit，并用 `git show --stat` 核对提交范围。

## 2. 完成不依赖实践的连续正文

- [x] 2.1 写出“B 快了 30%”开场与正确性合同推导，用同一 A/B 案例说明任务、输出工作量、随机性和质量接受条件；连续阅读时验证术语均在矛盾出现后引入。
- [x] 2.2 写出观察者、起止事件与异步完成推导，从客户端/服务端差值进入主机提交和设备完成反例；用事件手算核对正文中每个耗时均能从原始记录重建。
- [x] 2.3 写出冷启动、稳态、同条件工作负载和环境控制推导，说明 warm-up 排除规则为何取决于问题；用至少一组未删样本的连续运行记录验证冷启动与稳态结论可分别复核。
- [x] 2.4 写出从原始样本到中心与尾部差异的推导，再汇总最小 benchmark 协议和有边界的结论，并以“完成事件内部还有什么”自然引向第 01 章；人工核对协议每个字段都能回指前文某个已暴露缺口。
- [x] 2.5 对无独立实践版本执行一次 prose-only 编辑评审，删除重复标题、清单式复述和提前出现的结论，确认隐藏所有图示与交互后仍能沿一个主张完成因果推导，并把评审结论写入第 00 章编辑记录。
- [x] 2.6 为第 00 章增加结构和关键教学合同检查，运行正文相关测试、`npm run check:content` 和类型检查后提交一个独立 Git commit，并用 `git show --stat` 核对提交范围。

## 3. 让时间、状态与分布变化可观察

- [x] 3.1 实现观察边界尺，在同一原始事件轴上切换客户端、服务端和设备窗口并显示纳入区间；用组件测试验证切换只改变观察者和派生耗时，不改变底层事件。
- [x] 3.2 实现主机/设备双轨图，支持暂停和单步推进提交返回、设备完成与结果可读事件；用组件测试验证错误结束点会明确显示仍在途的工作。
- [x] 3.3 实现运行序列胶片，分别观察冷启动、稳态和未经声明的删样本行为；用确定性测试验证每次运行的一次性成本、保留状态和汇总输入一致。
- [x] 3.4 实现保留原始点的 A/B 分布刻度，呈现“中心更好而尾部更差”的反例；用单元与组件测试核对排序、中心值、尾部样本和失败数不会被视图切换丢失。
- [x] 3.5 将四类图示放在对应概念形成之后，补齐键盘操作、暂停/单步、reduced-motion、窄屏布局和深浅背景对比；通过自动化可访问性断言，并在桌面与移动视口实际检查标题、刻度和控制不被裁切。
- [x] 3.6 运行图示测试、样式测试、类型检查和生产构建，确认视觉组件没有复制第 01 章构图且每个图只回答一个认知问题后提交一个独立 Git commit，并用 `git show --stat` 核对提交范围。

## 4. 增加从属实践与迁移验收

- [ ] 4.1 在完整正文之后实现短计时窗口推演，让学习者先预测、再从未命名事件选择起止边界，并对缺失完成事件拒绝生成伪精确数字；用组件测试验证预测先于观察、原始事件始终可见且结果标注为模拟证据。
- [ ] 4.2 复查计时窗口推演相对双轨图的认识增量，确认它要求学习者主动重建测量合同而非复读既定动画；把保留理由和移除后正文仍完整的结论记录到编辑文档。
- [ ] 4.3 实现新的缺陷性能报告验收，按顺序要求识别首个致命缺口、提出修复协议并写出有边界结论；用测试验证结构化首问反馈和展开式检查标准分别覆盖任务合同、事件边界、工作负载、原始证据与结论范围。
- [ ] 4.4 将实践与验收接入正文末尾，确认页面访问、动画点击或实践运行不会产生掌握状态，且任何模拟结果旁均说明不能证明真实 GPU 性能；通过 DOM 测试和人工文本检查验证。
- [ ] 4.5 运行实践、验收、内容和类型相关测试及生产构建，确认本组只增加正文之后的能力检查后提交一个独立 Git commit，并用 `git show --stat` 核对提交范围。

## 5. 完成整章交付与验收

- [ ] 5.1 更新 README 的当前可查看内容、启动路径和 00→01 阅读顺序，并补齐第 00 章到第 01 章的前后章导航；用链接检查和路由测试验证文档中的入口均有效。
- [ ] 5.2 在桌面和移动视口完整阅读第 00 章，逐项检查主张连续性、标题密度、图文出现顺序、交互状态、窄屏文字可见性与 reduced-motion 退化，并在编辑记录中记录发现和修正结果。
- [ ] 5.3 运行 `npm run check` 和 `openspec validate build-trustworthy-baseline-chapter --strict`，确认第 00、01 章均通过内容、类型、测试和生产构建检查，且工作树中没有误纳入既有的无关改动。
- [ ] 5.4 更新任务完成状态并提交最终交付 Git commit，用 `git log` 与 `git status --short` 确认每个实施组均有独立提交且未提交既有 `package-lock.json` 改动，然后交由用户进行章节体验验收。
