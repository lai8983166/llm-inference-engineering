## 1. 建立第 06 章载体与过载底座

- [x] 1.1 新建第 06 章来源与编辑记录，按“稳定准入/抢占原理、版本锁定的框架实现示例、必须实测的判断、确定性教学假设”分类；核对 vLLM 抢占文档（默认 RECOMPUTE、抢占最低优先级）与 vLLM 调度器实现，逐项记录能支持和不能推出的结论。
- [x] 1.2 实现 `OverloadPolicy`、`OverloadEvent`、`OverloadTickSnapshot` 数据边界，四条策略（无界排队、按满拒绝、水位拒绝 W=1、抢占重计算）轨迹生成、`overloadSummary` 成本汇总与轨迹校验器；用单元测试验证固定突发下手算结局（7/5/5/7 拍、被拒 0/1/1/0、被抢占 0/0/0/1、重计算 0/0/0/6 unit、`P-d` 首执行 t5/被拒/t2/t3）、抢占只发生在空闲不足时、重计算 token 数等于 prompt 加已生成数，且数据不含真实耗时或 GPU 利用率字段。
- [x] 1.3 新建第 06 章 MDX、页面载体和稳定路由，接入首页入口并为第 05 章补下一章导航；用路由测试验证 00→01→02→03→04→05→06 均可直达、第 06 章能返回第 05 章且不会生成尚不存在的第 07 章链接。
- [x] 1.4 运行领域模型、路由、内容与类型检查，确认本组只建立载体、来源记录和确定性过载底座后提交独立 Git commit；用 `git show --stat` 核对没有纳入既有 `package-lock.json` 改动。

## 2. 完成从池见底到四张账单的正文

- [x] 2.1 用突发让池见底（`P-a`/`P-b` 同拍、`P-c` 恰好用掉最后 2 块、`P-d` 面对空池），沿无界排队轨迹写出等待成为常态、队列增长与先来后到被容量可行性打断；人工核对正文没有以任何准入机制作为出发点。
- [x] 2.2 把准入重述为成本分配，并列按满拒绝与水位拒绝两条轨迹；用同一时刻对大/小请求的相反裁决说明水位保护在跑者的余量；用内容测试锁定“承担者”表述先于任何策略名称。
- [x] 2.3 写出抢占重计算轨迹：受害者按“最近开始生成者”选取（登记为教学假设）、被抢占者以重新 prefill 恢复；显式回指第 03 章无缓存重算路径，重计算成本按重复 prefill 的 token 数计数；真实 swap 只命名不建模。
- [x] 2.4 以定性讨论与一次手算变体呈现优先级旋钮（出队顺序与抢占受害者），引用版本锁定的真实优先级抢占示例；核对不声称任何优先级方案公平或最优。
- [x] 2.5 以四张账单对照收束（总拍数、被拒、被抢占、重计算、最大排队、首执行拍），说明策略编码服务承诺且没有免费方向；吞吐、尾延迟与公平性登记为测量问题；登记“等待者被客户端放弃持有无效希望”作为第 07 章引子。
- [x] 2.6 隐藏所有图示与实践执行 prose-only 编辑评审，删除公式卡堆、并列优缺点、重复小结和脱离因果链的工程附录，确认五个标题只对应认知任务改变；将修正写入编辑记录，运行正文结构、固定算例、术语顺序、内容与类型检查后提交独立 Git commit。

## 3. 让轨迹演化与成本对照可观察

- [x] 3.1 实现“过载时的池与队伍”图：逐拍双条带（空闲块数、排队深度）加事件账本，策略与水位可切换，重计算事件显式标注；用组件测试验证读数来自轨迹、被拒/被抢占/排队事件可区分。
- [x] 3.2 实现“四种策略，四张账单”图：策略为行、成本计数为列的对照账本，每行高亮成本承担者；用单元与组件测试核对全部计数来自 `overloadSummary`。
- [x] 3.3 将两图分别放在轨迹推导与账单对照形成之后，补齐暂停、前后单步、重置、键盘、当前拍、原始计数、文字/图案编码、reduced-motion 与窄屏内部滚动；通过可访问性和样式测试验证控制不遮标题、颜色不是唯一编码且拍数不被标成时间。
- [x] 3.4 在桌面与移动视口实际检查两张图的完整过程，确认双条带、队列深度、重计算标注与账单行不被裁切，隐藏任一图也不破坏正文结论；把发现、修正和人工证据写入第 06 章编辑记录。
- [x] 3.5 运行图示、领域模型、样式、内容、类型与生产构建检查，确认两图分别只回答轨迹演化和成本对照且未复制前章构图后提交独立 Git commit；用 `git show --stat` 核对提交范围。

## 4. 决定从属推演并完成迁移验收

- [x] 4.1 在完整正文之后实现候选过载账本审查（新突发工作量与更小池，含真实的拒绝或抢占事件），先让学习者判断首个被拒/被抢占请求的成本由谁承担，再重建一次准入裁决顺序（到达 → 计算 free/need/水位或抢占裁决 → 准入/排队/拒绝/抢占 → 登记 → 执行）；用组件测试验证预测先于解释、原始账本全程可见且每个结果标为模拟证据。
- [x] 4.2 对照两张解释图评审候选实践的认识增量：只有它要求主动组合决定点、容量输入与成本承担者归因而非复读图示时才保留，否则删除；把决定理由以及移除实践后正文仍完整的结论写入编辑记录。
- [x] 4.3 实现不同于正文的迁移评估，换用突发、水位与池规模，依次检查一个致命归因错误（排队当不公平或重计算当免费）、承担者归因、重计算 token 复算、水位权衡、证据边界与待解问题；用测试验证结构化首问和展开标准覆盖六个维度且不以背诵术语为完成证据。
- [x] 4.4 将保留的实践和迁移评估放在完整正文之后，确认访问页面、播放图示、展开答案或填写自由文本不会生成掌握状态；通过 DOM、存储写入和人工文本检查验证所有拍数/计数旁均说明不能证明真实延迟或性能。
- [x] 4.5 运行实践、评估、内容、类型和生产构建检查，确认本组只增加主动重建与迁移诊断证据后提交独立 Git commit；用 `git show --stat` 核对提交范围。

## 5. 完成第 06 章交付与验收

- [x] 5.1 更新 README 当前内容和 00→01→02→03→04→05→06 阅读顺序，完成首页与第 05、06 章前后导航；用链接检查和路由测试验证已发布入口全部有效且未链接未实现的第 07 章。
- [x] 5.2 在桌面与移动视口完整阅读第 06 章，逐项检查固定模型/请求/块参数连续性、标题密度、手算可复算性、概念命名时机、图文顺序、交互状态、窄屏可见性和 reduced-motion；在编辑记录中记录发现、修正与最终判断。
- [x] 5.3 运行 `npm run check` 和 `openspec validate build-overload-admission-chapter --strict`，确认第 00—06 章均通过内容、类型、测试与生产构建，且拍数、计数与等待始终是证据边界内的记账。
- [x] 5.4 更新任务完成状态并提交最终交付 Git commit，用 `git log` 与 `git status --short` 确认各实施组均有独立提交且没有误纳入既有 `package-lock.json` 改动，然后交由用户体验验收并归档。
