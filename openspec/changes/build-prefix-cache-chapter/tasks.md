## 1. 建立第 10 章载体与缓存底座

- [ ] 1.1 新建第 10 章来源与编辑记录，按“稳定缓存/共享原理、版本锁定的框架实现示例、必须实测的判断、确定性教学假设”分类；核对 vLLM APC 文档与设计文档（哈希链、只缓存整块、ref_cnt、自由队列逐出、cache_salt）及 PagedAttention 论文的引用计数与写时复制语句，逐项记录能支持和不能推出的结论。
- [ ] 1.2 实现 `prefixCacheTrace` 领域模型：块状态（free/private/shared/cached + refCount）、命中/未中/逐出事件、释放三分（归还/递减/转缓存）、双策略轨迹（no-cache/prefix-cache）、校验器（rc>0 永不逐出、归还只在 rc=0、命中只对块对齐前缀、每请求恰一次终结）与 outcome/hitRate 纯函数；用单元测试验证手算结局（两轨迹各 7 拍、S-c 首执行 t3→t2、B0 rc 序列 1→2→1→0→缓存→t6 逐出、命中率 1/3）。
- [ ] 1.3 新建第 10 章 MDX、页面载体和稳定路由，接入首页入口并为第 09 章补下一章导航；用路由测试验证 00→…→09→10 均可直达、第 10 章能返回第 09 章且不会生成尚不存在的第 11 章链接。
- [ ] 1.4 运行领域模型、路由、内容与类型检查，确认本组只建立载体、来源记录和确定性缓存底座后提交独立 Git commit；用 `git show --stat` 核对没有纳入既有 `package-lock.json` 改动。

## 2. 完成从重复计算到组合合同的正文

- [ ] 2.1 从两请求共享前缀各自重算的反例出发推导缓存动机，用块对齐（floor(前缀长/块大小)）限制命中并配毛边小反例；人工核对正文没有以“缓存加速”作为出发点。
- [ ] 2.2 手算并列无缓存与缓存两条轨迹：命中方少拿 1 块、S-c 提前一拍准入、S-d 的 5 块需求驱逐缓存块；命中率 1/3 按第 08 章口径入指标；核对不把命中率当收益。
- [ ] 2.3 写出共享块生命周期：命中 rc++、完成 rc−− 不归还、最后所有者 rc=0 转缓存、取消同理；第 07 章无泄漏合同重写为“递减或归还”；写时复制按论文语句命名不建模。
- [ ] 2.4 呈现逐出与预算的拉锯：rc=0 缓存块占池、LRU 逐出、rc>0 永不逐出；缓存感知调度与公平的张力定性讨论（回指第 06 章）；不评判哪种取舍。
- [ ] 2.5 以组合合同清单收束：所有权、失效/逐出、调度交互、证据；机制叠加放大复杂度而非线性叠加；多机制与真实框架留给深化分支与第 11 章。
- [ ] 2.6 隐藏所有图示与实践执行 prose-only 编辑评审，删除公式卡堆、并列优缺点、重复小结和脱离因果链的工程附录，确认五个标题只对应认知任务改变；将修正写入编辑记录，运行正文结构、固定算例、术语顺序、内容与类型检查后提交独立 Git commit。

## 3. 让共享账本与对照结局可观察

- [ ] 3.1 实现“共享块的引用计数”图：块带 rc 徽章，沿轨迹推进显示命中、递减、转缓存与逐出的完整生命周期；用组件测试验证 rc 序列与状态均来自轨迹。
- [ ] 3.2 实现“命中改写准入”图：无缓存与缓存两条时间线的空闲块条带并排，标注命中、S-c 提前一拍与 S-d 逐出；用单元与组件测试核对全部拍数与结局来自同一纯函数族。
- [ ] 3.3 将两图分别放在所有权与对照形成之后，补齐暂停、前后单步、重置、键盘、当前拍、rc 徽章、文字/图案编码、reduced-motion 与窄屏内部滚动；通过可访问性和样式测试验证控制不遮标题、颜色不是唯一编码且拍数不被标成时间。
- [ ] 3.4 在桌面与移动视口实际检查两张图的完整过程，确认 rc 徽章、命中/逐出标注、条带与拍差标注不被裁切，隐藏任一图也不破坏正文结论；把发现、修正和人工证据写入第 10 章编辑记录。
- [ ] 3.5 运行图示、领域模型、样式、内容、类型与生产构建检查，确认两图分别只回答共享账本和对照结局且未复制前章构图后提交独立 Git commit；用 `git show --stat` 核对提交范围。

## 4. 决定从属推演并完成迁移验收

- [ ] 4.1 在完整正文之后实现候选缓存账本审查（含一次命中、一次 rc 递减不归还、一次逐出），先让学习者判断“S-b 完成时 B0 发生了什么”，再重建共享块完整生命周期顺序（命中 rc++ → 追加使用 → 完成 rc−− → rc=0 转缓存 → 压力下逐出）；用组件测试验证预测先于解释、原始账本全程可见且每个结果标为模拟证据。
- [ ] 4.2 对照两张解释图评审候选实践的认识增量：只有它要求主动组合对齐、所有权与逐出条件而非复读图示时才保留，否则删除；把决定理由以及移除实践后正文仍完整的结论写入编辑记录。
- [ ] 4.3 实现不同于正文的迁移评估，换用毛边前缀（5-token 只共享 1 块）与取消共享方的组合，依次检查一个致命归因错误（“完成即释放”或“缓存块可随时逐出”）、共享块数复算、rc 序列、逐出条件、调度张力与组合合同；用测试验证结构化首问和展开标准覆盖六个维度且不以背诵机制名称为完成证据。
- [ ] 4.4 将保留的实践和迁移评估放在完整正文之后，确认访问页面、播放图示、展开答案或填写自由文本不会生成掌握状态；通过 DOM、存储写入和人工文本检查验证所有拍数/rc 旁均说明不能证明真实命中率或收益。
- [ ] 4.5 运行实践、评估、内容、类型和生产构建检查，确认本组只增加主动重建与迁移诊断证据后提交独立 Git commit；用 `git show --stat` 核对提交范围。

## 5. 完成第 10 章交付与验收

- [ ] 5.1 更新 README 当前内容和 00→…→10 阅读顺序，完成首页与第 09、10 章前后导航；用链接检查和路由测试验证已发布入口全部有效且未链接未实现的第 11 章。
- [ ] 5.2 在桌面与移动视口完整阅读第 10 章，逐项检查固定工作量/池/前缀参数连续性、标题密度、手算可复算性、概念命名时机、图文顺序、交互状态、窄屏可见性和 reduced-motion；在编辑记录中记录发现、修正与最终判断。
- [ ] 5.3 运行 `npm run check` 和 `openspec validate build-prefix-cache-chapter --strict`，确认第 00—10 章均通过内容、类型、测试与生产构建，且 rc、命中率与拍数始终是证据边界内的记账。
- [ ] 5.4 更新任务完成状态并提交最终交付 Git commit，用 `git log` 与 `git status --short` 确认各实施组均有独立提交且没有误纳入既有 `package-lock.json` 改动，然后交由用户体验验收并归档。
