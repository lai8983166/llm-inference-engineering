## 1. 建立多请求章节载体与确定性事件底座

- [x] 1.1 新建第 02 章来源与编辑记录，按“稳定原理、运行时/框架事实、必须实测的判断”分类正文依据；逐项核对并记录一手来源、版本边界或明确的教学模型假设。
- [x] 1.2 实现最小 `RequestFixture`、`RequestSnapshot`、`ExecutionGroup`、`ConcurrencyEvent` 与 `StrategyTrace` 数据边界，以及从固定请求生成串行、独立循环和静态 batch 逻辑轨迹的纯函数；用单元测试验证到达不变、请求级状态独立、事件顺序合法和模拟数据不含真实耗时字段。
- [x] 1.3 新建第 02 章 MDX 与稳定路由，接入现有章节注册和首页入口，并为第 01 章增加有效的下一章导航；用路由测试验证 00、01、02 均可直达且没有生成尚不存在的第 03 章链接。
- [x] 1.4 运行本组领域测试、路由测试、`npm run check:content` 与类型检查，确认只包含第 02 章载体和事件底座后提交独立 Git commit，并用 `git show --stat` 核对提交范围未包含既有 `package-lock.json` 改动。

## 2. 完成由单请求循环推向调度问题的连续正文

- [x] 2.1 写出固定长短请求的到达场景，并从第 01 章逐项继承请求状态、KV 使用权、输出许可、终止原因和清理责任；人工核对后续三种方案始终使用同一工作负载且没有先抛并发术语。
- [x] 2.2 写出串行队列的合理性与队头阻塞推导，让晚到短请求的等待能够从原始事件手算重建；核对正文明确区分模型执行工作和排队等待，且没有把串行方案写成稻草人。
- [x] 2.3 从“每请求一个线程或协程”继续推导，区分主机控制流交错、设备任务执行和批量模型执行；用一组设备事件反例核对正文没有从协程活跃武断推出 kernel 并行或性能收益。
- [x] 2.4 从合并已等待请求的动机推导静态 batch，分别用 prompt 对齐、decode 提前结束和晚到请求说明 padding、空槽与封闭批次等待；手算核对每个有效 token、占位位置和批外等待均能回到固定案例。
- [x] 2.5 汇总下一次执行选择必须知道的可运行状态、phase、形状、在途工作与请求不变量，并自然引出“先理解不断增长的 KV 状态与容量”；人工核对正文止于问题合同，没有提前教授 continuous batching、分页、抢占或公平策略。
- [x] 2.6 对隐藏全部图示与可操作实践的版本执行 prose-only 编辑评审，删除并列优缺点卡、重复标题、清单式复述和过早结论，确认读者能沿同一组请求完成五次认知转折；把评审发现和修正写入编辑记录。
- [x] 2.7 增加章节结构、术语顺序、固定案例与证据边界检查，运行正文相关测试、`npm run check:content` 和类型检查后提交独立 Git commit，并用 `git show --stat` 核对提交范围。

## 3. 让控制流、设备事件与 batch 占用可观察

- [x] 3.1 实现请求/设备因果轨迹图，在固定到达事件下切换串行和独立循环并支持暂停、单步、重置；用组件测试验证策略切换不改变请求工作量，主机活跃与设备开始使用不同状态，且界面不产生未经模型定义的重叠结论。
- [x] 3.2 实现静态 batch 占用图，逐步显示 prefill 有效 token 与 padding、decode 有效成员与空槽、以及批外等待的新请求；用单元和组件测试核对每格分类、成员终止、历史保留和汇总计数来自同一事件数据。
- [x] 3.3 将两张图分别放在对应矛盾形成之后，补齐键盘操作、暂停/单步、reduced-motion、窄屏内部滚动和深浅背景文字对比；通过可访问性断言和样式测试验证标题、标签、当前事件与静态说明始终可读。
- [x] 3.4 在桌面和移动视口实际检查完整图示过程，确认控制不会遮盖标题、时间标签不被裁切、颜色不是唯一编码，且隐藏任一图不会破坏正文结论；将发现和修正结果写入第 02 章编辑记录。
- [x] 3.5 运行图示、数据、样式、内容与类型测试及生产构建，确认两张图各自只回答一个认知问题且没有复制前两章构图后提交独立 Git commit，并用 `git show --stat` 核对提交范围。

## 4. 决定从属推演并完成迁移验收

- [x] 4.1 在完整正文之后实现候选短轨迹审查，让学习者先预测最早的等待或浪费，再把原始事件归类为不可运行、可运行未选择、有效设备工作或 padding/空槽；用组件测试验证预测先于解释、历史事件始终可见且所有结果标为模拟证据。
- [x] 4.2 对照两张解释图评审轨迹审查的认识增量：只有它要求主动重建事件状态而非复读动画时才保留，否则删除组件；把保留或移除理由以及拿掉实践后正文仍完整的结论写入编辑记录。
- [x] 4.3 实现不同于正文案例的迁移评估，依次检查首个因果缺口、主机并发与设备执行的区分、请求级不变量、证据边界及下一次选择所需状态；用测试验证结构化首问和展开式检查标准覆盖五个维度且不询问方案名称。
- [x] 4.4 将保留下来的实践和迁移评估放在正文推导之后，确认页面访问、动画播放或答案展开不会生成掌握状态，并在每个逻辑步结果旁说明它不能证明真实 GPU 性能；通过 DOM 测试和人工文本检查验证。
- [x] 4.5 运行实践、评估、内容、类型和生产构建检查，确认本组只增加主动重建与迁移证据后提交独立 Git commit，并用 `git show --stat` 核对提交范围。

## 5. 完成第 02 章交付与验收

- [x] 5.1 更新 README 的当前可查看内容和 00→01→02 阅读顺序，完成第 01、02 章前后章导航与首页文案；用链接检查和路由测试验证所有已发布入口有效且未链接未实现章节。
- [x] 5.2 在桌面和移动视口完整阅读第 02 章，逐项检查固定案例连续性、标题密度、概念命名时机、图文顺序、交互状态、窄屏可见性和 reduced-motion 退化；在编辑记录中记录发现、修正与最终判断。
- [x] 5.3 运行 `npm run check` 和 `openspec validate build-naive-concurrency-chapter --strict`，确认第 00—02 章均通过内容、类型、测试与生产构建检查，且所有模拟结论保持证据边界。
- [x] 5.4 更新任务完成状态并提交最终交付 Git commit，用 `git log` 与 `git status --short` 确认每个实施组均有独立提交且没有误纳入既有 `package-lock.json` 改动，然后交由用户体验验收。
