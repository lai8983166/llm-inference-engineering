## 1. 建立第 08 章载体与指标底座

- [x] 1.1 新建第 08 章来源与编辑记录，按"稳定统计/负载原理、版本锁定的框架实现示例、必须实测的判断、确定性教学假设"分类；核对 The Tail at Scale 与 vLLM 压测文档（request-rate、max-concurrency、client_queue_time 单列），逐项记录能支持和不能推出的结论。
- [x] 1.2 实现 `requestMetrics`、`distribution`/`percentile`（最近邻秩，登记定义）、`sloVerdict`、`goodputSummary`、`meanTailCounterexample` 与开环/闭环到达表生成及对照纯函数；用单元测试验证固定工作量（约 6 请求、6 块池、水位 W=1）下每请求指标由事件差独立复算、反例的 A 均值更好而 B 的 p99 与 SLO 判定更好、闭环对照的队列深度与首 token 差异手算可复核，且数据不含真实耗时字段。
- [x] 1.3 新建第 08 章 MDX、页面载体和稳定路由，接入首页入口并为第 07 章补下一章导航；用路由测试验证 00→…→07→08 均可直达、第 08 章能返回第 07 章且不会生成尚不存在的第 09 章链接。
- [x] 1.4 运行领域模型、路由、内容与类型检查，确认本组只建立载体、来源记录和确定性指标底座后提交独立 Git commit；用 `git show --stat` 核对没有纳入既有 `package-lock.json` 改动。

## 2. 完成从事件对到聚合链的正文

- [x] 2.1 从"快不快"的含糊出发，用钉死的事件对定义排队、首 token、间隔与端到端，每个定义配"换边界换数值"的小反例；人工核对正文没有以任何指标名词作为出发点。
- [x] 2.2 手算均值更好但 SLO 更差的反例：分布表、p50/p99、SLO 判定翻转；说明均值回答总量、分位回答承诺，两者不替代；用内容测试锁定分布先于任何 SLO 结论。
- [x] 2.3 并列开环与闭环的完整对照表：到达过程、队列深度、各指标差异；闭环隐藏排队与"测不出过载"如实呈现；引用版本锁定的真实压测参数示例；核对不声称某种环式正确。
- [x] 2.4 用终态原因定义 goodput：吞吐与 goodput 分叉表，被拒/失败/被放弃逐项列出；完成数不是服务质量充分指标的结论回指第 06/07 章。
- [x] 2.5 以可回溯聚合链收束：原始事件 → 每请求 → 分布 → 判定的固定顺序与报告模板；分位审计能走回头路；指标异常的归因指向（排队/prefill/decode/终止）登记为第 09 章入口。
- [x] 2.6 隐藏所有图示与实践执行 prose-only 编辑评审，删除公式卡堆、并列优缺点、重复小结和脱离因果链的工程附录，确认五个标题只对应认知任务改变；将修正写入编辑记录，运行正文结构、固定算例、术语顺序、内容与类型检查后提交独立 Git commit。

## 3. 让聚合链与环式对照可观察

- [x] 3.1 实现"从事件到分布"图：单请求事件尺（可切换）+ 指标切换（排队/首 token/间隔/端到端）+ 全体取值排序条带与均值/p50/p99/SLO 线，A/B 系统切换呈现反例；用组件测试验证读数来自领域模型。
- [x] 3.2 实现"开环与闭环"图：同一服务两条时间线并排，到达标记与队列深度条带对照，读数给出两环指标差异；用单元与组件测试核对全部数字来自同一纯函数族。
- [x] 3.3 将两图分别放在分布与环式对照形成之后，补齐暂停、前后单步、重置、键盘、当前拍、原始计数、文字/图案编码、reduced-motion 与窄屏内部滚动；通过可访问性和样式测试验证控制不遮标题、颜色不是唯一编码且拍数不被标成时间。
- [ ] 3.4 在桌面与移动视口实际检查两张图的完整过程，确认事件尺、分位标记、SLO 线、到达标记与队列条带不被裁切，隐藏任一图也不破坏正文结论；把发现、修正和人工证据写入第 08 章编辑记录。
- [ ] 3.5 运行图示、领域模型、样式、内容、类型与生产构建检查，确认两图分别只回答聚合链和环式对照且未复制前章构图后提交独立 Git commit；用 `git show --stat` 核对提交范围。

## 4. 决定从属推演并完成迁移验收

- [ ] 4.1 在完整正文之后实现候选指标报告审查（新工作量与配套原始事件），先让学习者找出报告里的定义错误（首 token 从准入起算、只报均值），再重建聚合链顺序；用组件测试验证预测先于解释、原始事件与报告全程可见且每个结果标为模拟证据。
- [ ] 4.2 对照两张解释图评审候选实践的认识增量：只有它要求主动组合定义、分布与口径而非复读图示时才保留，否则删除；把决定理由以及移除实践后正文仍完整的结论写入编辑记录。
- [ ] 4.3 实现不同于正文的迁移评估，换用闭环负载与含拒绝/失败的工作量，依次检查一个致命口径错误（闭环宣称零排队或吞吐当 goodput）、定义核对、分位复算、环式识别、证据边界与归因指向；用测试验证结构化首问和展开标准覆盖六个维度且不以背诵指标名词为完成证据。
- [ ] 4.4 将保留的实践和迁移评估放在完整正文之后，确认访问页面、播放图示、展开答案或填写自由文本不会生成掌握状态；通过 DOM、存储写入和人工文本检查验证所有拍数/分位旁均说明不能证明真实延迟或性能。
- [ ] 4.5 运行实践、评估、内容、类型和生产构建检查，确认本组只增加主动重建与迁移诊断证据后提交独立 Git commit；用 `git show --stat` 核对提交范围。

## 5. 完成第 08 章交付与验收

- [ ] 5.1 更新 README 当前内容和 00→…→08 阅读顺序，完成首页与第 07、08 章前后导航；用链接检查和路由测试验证已发布入口全部有效且未链接未实现的第 09 章。
- [ ] 5.2 在桌面与移动视口完整阅读第 08 章，逐项检查固定工作量/池/环式连续性、标题密度、手算可复算性、概念命名时机、图文顺序、交互状态、窄屏可见性和 reduced-motion；在编辑记录中记录发现、修正与最终判断。
- [ ] 5.3 运行 `npm run check` 和 `openspec validate build-metrics-evidence-chapter --strict`，确认第 00—08 章均通过内容、类型、测试与生产构建，且指标、分位与拍数始终是证据边界内的记账。
- [ ] 5.4 更新任务完成状态并提交最终交付 Git commit，用 `git log` 与 `git status --short` 确认各实施组均有独立提交且没有误纳入既有 `package-lock.json` 改动，然后交由用户体验验收并归档。
