## 1. 建立第 09 章载体与预算底座

- [x] 1.1 新建第 09 章来源与编辑记录，按“稳定算术/归因原理、版本锁定的框架实现示例、必须实测的判断、确定性教学假设”分类；核对 Amdahl 1967 原始文献与 vLLM profiling 文档（profiler 取舍、观测者效应、--profile 旗标），逐项记录能支持和不能推出的结论。
- [x] 1.2 实现 `StepBudget`、`optimize`、`total`、`speedup`、`ceiling`、固定优化表（更快 kernel/CUDA Graph/KV 量化/去同步）与 `noiseCases` 固定样本表及范围重叠判定纯函数；用单元测试验证基线 20 单位下四优化的收益（20/19、20/14、20/17、20/18）与上限（compute 1.11、launch 1.67、memory 1.43）、重叠样本不可声称与分离样本可支持，且数据不含真实耗时字段。
- [x] 1.3 新建第 09 章 MDX、页面载体和稳定路由，接入首页入口并为第 08 章补下一章导航；用路由测试验证 00→…→08→09 均可直达、第 09 章能返回第 08 章且不会生成尚不存在的第 10 章链接。
- [x] 1.4 运行领域模型、路由、内容与类型检查，确认本组只建立载体、来源记录和确定性预算底座后提交独立 Git commit；用 `git show --stat` 核对没有纳入既有 `package-lock.json` 改动。

## 2. 完成从局部反例到归因链的正文

- [x] 2.1 用“kernel 减半而服务快 5%”的预算反例开篇：五部件之和、占比决定收益；人工核对正文没有以任何 profiler 工具作为出发点。
- [x] 2.2 建立五种瓶颈指纹与对应优化；真实 profiler 的取舍（低开销 vs 富信息）与观测者效应以版本锁定示例呈现；核对指纹先于优化推荐。
- [x] 2.3 手算 Amdahl 上限表：各部件归零的上限 `1/(1−占比)`，“先查上限再动手”成文；登记串行求和是教学简化、重叠只会让局部收益更难兑现。
- [x] 2.4 用固定样本表呈现噪声判定：范围重叠不可声称、分离仍需同条件；最小对照实验纪律回指第 00 章合同、分布口径回指第 08 章。
- [x] 2.5 以归因链收束共同主线：指标 → 层 → 指纹 → 假设 → 最小实验 → 确认或推翻；深化分支与第 11 章真实框架登记为后续。
- [x] 2.6 隐藏所有图示与实践执行 prose-only 编辑评审，删除公式卡堆、并列优缺点、重复小结和脱离因果链的工程附录，确认五个标题只对应认知任务改变；将修正写入编辑记录，运行正文结构、固定算例、术语顺序、内容与类型检查后提交独立 Git commit。

## 3. 让收益构成与噪声判定可观察

- [x] 3.1 实现“收益去哪了”图：基线预算堆叠条 + 优化切换后的新堆叠条、收益读数与上限对照，数字全部来自预算纯函数；用组件测试验证四种优化的收益与上限读数。
- [x] 3.2 实现“噪声与对照”图：两组固定样本的取值条带与范围括号、重叠/分离判定与结论模板；用组件测试验证判定来自样本表且结论模板随判定切换。
- [x] 3.3 将两图分别放在上限推导与噪声判定形成之后，补齐切换、键盘、当前读数、原始单位、文字/图案编码、reduced-motion 与窄屏内部滚动；通过可访问性和样式测试验证控制不遮标题、颜色不是唯一编码且单位不被标成时间。
- [x] 3.4 在桌面与移动视口实际检查两张图的完整过程，确认堆叠分段、收益读数、样本条带与范围括号不被裁切，隐藏任一图也不破坏正文结论；把发现、修正和人工证据写入第 09 章编辑记录。
- [x] 3.5 运行图示、领域模型、样式、内容、类型与生产构建检查，确认两图分别只回答收益构成和噪声判定且未复制前章构图后提交独立 Git commit；用 `git show --stat` 核对提交范围。

## 4. 决定从属推演并完成迁移验收

- [x] 4.1 在完整正文之后实现候选优化报告审查（kernel 减半、样本范围重叠、端到端声称提升的报告），先让学习者判断收益为何没兑现（占比、噪声或指纹错配），再重建归因链顺序（指标 → 层 → 指纹 → 假设 → 最小实验 → 确认）；用组件测试验证预测先于解释、报告与预算全程可见且每个结果标为模拟证据。
- [x] 4.2 对照两张解释图评审候选实践的认识增量：只有它要求主动组合预算、指纹与噪声判定而非复读图示时才保留，否则删除；把决定理由以及移除实践后正文仍完整的结论写入编辑记录。
- [x] 4.3 实现不同于正文的迁移评估，换用占比颠倒的新预算表与新样本组合，依次检查一个致命归因错误（“kernel 快了服务必然快”或“范围重叠也算改进”）、收益复算、上限复算、指纹选择、噪声判定与归因链设计；用测试验证结构化首问和展开标准覆盖六个维度且不以背诵定律名称为完成证据。
- [x] 4.4 将保留的实践和迁移评估放在完整正文之后，确认访问页面、切换图示、展开答案或填写自由文本不会生成掌握状态；通过 DOM、存储写入和人工文本检查验证所有单位/样本旁均说明不能证明真实延迟或收益。
- [x] 4.5 运行实践、评估、内容、类型和生产构建检查，确认本组只增加主动重建与迁移诊断证据后提交独立 Git commit；用 `git show --stat` 核对提交范围。

## 5. 完成第 09 章交付与验收

- [x] 5.1 更新 README 当前内容和 00→…→09 阅读顺序，完成首页与第 08、09 章前后导航；用链接检查和路由测试验证已发布入口全部有效且未链接未实现的第 10 章。
- [x] 5.2 在桌面与移动视口完整阅读第 09 章，逐项检查固定预算/优化/样本连续性、标题密度、手算可复算性、概念命名时机、图文顺序、交互状态、窄屏可见性和 reduced-motion；在编辑记录中记录发现、修正与最终判断。
- [x] 5.3 运行 `npm run check` 和 `openspec validate build-optimization-attribution-chapter --strict`，确认第 00—09 章均通过内容、类型、测试与生产构建，且预算单位、收益与样本始终是证据边界内的记账。
- [x] 5.4 更新任务完成状态并提交最终交付 Git commit，用 `git log` 与 `git status --short` 确认各实施组均有独立提交且没有误纳入既有 `package-lock.json` 改动，然后交由用户体验验收并归档。
