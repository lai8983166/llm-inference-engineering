## 1. 建立第 07 章载体与终止底座

- [x] 1.1 新建第 07 章来源与编辑记录，按“稳定终止/清理原理、版本锁定的框架实现示例、必须实测的判断、确定性教学假设”分类；核对 vLLM abort 终态路径（`FINISHED_ABORTED` 经调度器统一收尾）与错误处理代码，逐项记录能支持和不能推出的结论。
- [x] 1.2 实现 `TerminationCause`、`TerminationInjection`、终止事件（terminated/left-queue/stream-closed）与 `TerminationTickSnapshot` 数据边界，带注入的轨迹生成、无终止基线生成、`noLeakIssues` 校验器与对照汇总纯函数；用单元测试验证固定注入下手算结局（基线 8 拍、注入终止 6 拍、`C-d` 首执行 t5→t4、幸存者完成拍 t4→t5）、每请求恰一终态、终态后零块不在队流已关，且数据不含真实耗时或 GPU 利用率字段。
- [x] 1.3 新建第 07 章 MDX、页面载体和稳定路由，接入首页入口并为第 06 章补下一章导航；用路由测试验证 00→01→02→03→04→05→06→07 均可直达、第 07 章能返回第 06 章且不会生成尚不存在的第 08 章链接。
- [x] 1.4 运行领域模型、路由、内容与类型检查，确认本组只建立载体、来源记录和确定性终止底座后提交独立 Git commit；用 `git show --stat` 核对没有纳入既有 `package-lock.json` 改动。

## 2. 完成从任意状态取消到无泄漏合同的正文

- [x] 2.1 从第 06 章“无效希望”场景开始，把取消写成状态与事件的组合：排队、等待 prefill、生成中、重算恢复各有转移；人工核对正文没有以取消 API 作为出发点。
- [x] 2.2 建立终态统一：六条终点路径一张清理仪式表（停止增长、在途安全、释放块、离队、关流、记录原因），部分输出合同回指第 01 章；用内容测试锁定仪式表先于清理成本讨论。
- [x] 2.3 手算并列基线与注入终止的完整对照：`C-b` 取消释放 2 块、`C-d` 提前一拍准入、`C-e` 超时离队、幸存者完成拍后移；说明终止改写第 05 章可运行集合且清理不免费。
- [x] 2.4 写出超时与断开作为隐式取消来源（显式调用、期限检查、连接事件，检测不同转移相同），引用版本锁定的真实 abort 终态示例；失败的爆炸半径按请求级与引擎级分级讨论并各给小算例，不评判哪种半径正确。
- [x] 2.5 以无泄漏合同与可观测终点收束：不变量清单由校验器逐事件核对，终态事件是第 08 章指标的证据源；“服务语义完备”由此获得可检验定义。
- [x] 2.6 隐藏所有图示与实践执行 prose-only 编辑评审，删除公式卡堆、并列优缺点、重复小结和脱离因果链的工程附录，确认五个标题只对应认知任务改变；将修正写入编辑记录，运行正文结构、固定算例、术语顺序、内容与类型检查后提交独立 Git commit。

## 3. 让终点覆盖与容量后果可观察

- [x] 3.1 实现“六条终点，同一次清理”图：生命周期泳道加终态原因编码，终点并排零块/离队/关流三枚清理对勾，逐拍推进显示清理事件紧随终态；用组件测试验证终态原因与清理步骤均来自轨迹。
- [x] 3.2 实现“终止也是容量回收”图：基线与注入终止两条轨迹的空闲块条带并排，标注释放与提前准入的拍差；用单元与组件测试核对全部拍数与结局来自同一纯函数族。
- [x] 3.3 将两图分别放在清理仪式与容量对照形成之后，补齐暂停、前后单步、重置、键盘、当前拍、原始计数、文字/图案编码、reduced-motion 与窄屏内部滚动；通过可访问性和样式测试验证控制不遮标题、颜色不是唯一编码且终态原因有文字标注。
- [ ] 3.4 在桌面与移动视口实际检查两张图的完整过程，确认终态标记、清理对勾、条带与拍差标注不被裁切，隐藏任一图也不破坏正文结论；把发现、修正和人工证据写入第 07 章编辑记录。
- [ ] 3.5 运行图示、领域模型、样式、内容、类型与生产构建检查，确认两图分别只回答终点覆盖和容量后果且未复制前章构图后提交独立 Git commit；用 `git show --stat` 核对提交范围。

## 4. 决定从属推演并完成迁移验收

- [x] 4.1 在完整正文之后实现候选终止账本审查（新工作量，含排队中超时与生成中断开两类终止），先让学习者判断每个终态释放了什么（块、队列位置、流，或什么都没有），再重建清理顺序；用组件测试验证预测先于解释、原始账本全程可见且每个结果标为模拟证据。
- [x] 4.2 对照两张解释图评审候选实践的认识增量：只有它要求主动组合状态覆盖、清理仪式与容量后果而非复读图示时才保留，否则删除；把决定理由以及移除实践后正文仍完整的结论写入编辑记录。
- [x] 4.3 实现不同于正文的迁移评估，换用断开、失败与超时的注入组合，依次检查一个致命归因错误（如“取消必须等 decode 完成所以总更慢”或“失败必杀全组”）、释放物判断、清理顺序、爆炸半径、证据边界与可观测终点；用测试验证结构化首问和展开标准覆盖六个维度且不以背诵术语为完成证据。
- [x] 4.4 将保留的实践和迁移评估放在完整正文之后，确认访问页面、播放图示、展开答案或填写自由文本不会生成掌握状态；通过 DOM、存储写入和人工文本检查验证所有拍数/终态旁均说明不能证明真实延迟或可靠性。
- [x] 4.5 运行实践、评估、内容、类型和生产构建检查，确认本组只增加主动重建与迁移诊断证据后提交独立 Git commit；用 `git show --stat` 核对提交范围。

## 5. 完成第 07 章交付与验收

- [x] 5.1 更新 README 当前内容和 00→01→02→03→04→05→06→07 阅读顺序，完成首页与第 06、07 章前后导航；用链接检查和路由测试验证已发布入口全部有效且未链接未实现的第 08 章。
- [ ] 5.2 在桌面与移动视口完整阅读第 07 章，逐项检查固定模型/请求/注入连续性、标题密度、手算可复算性、概念命名时机、图文顺序、交互状态、窄屏可见性和 reduced-motion；在编辑记录中记录发现、修正与最终判断。
- [x] 5.3 运行 `npm run check` 和 `openspec validate build-service-termination-chapter --strict`，确认第 00—07 章均通过内容、类型、测试与生产构建，且终态、清理与拍数始终是证据边界内的记账。
- [ ] 5.4 更新任务完成状态并提交最终交付 Git commit，用 `git log` 与 `git status --short` 确认各实施组均有独立提交且没有误纳入既有 `package-lock.json` 改动，然后交由用户体验验收并归档。
