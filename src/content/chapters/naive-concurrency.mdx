多个请求进入同一个服务时，第 01 章建立的请求状态、输出许可和资源所有权都不能消失。现在只增加一个条件：`R-long` 尚未结束，`R-short` 和 `R-late` 已经先后到达。接下来的正文将保持这三份请求合同不变，只改变它们等待和执行的组织方式。

<h2 id="multiple-requests">当第二个请求到达</h2>

单请求循环仍然正确，困难来自它不再独占整个服务。我们需要先保留每个请求的原始到达与逻辑工作，再判断新的等待究竟由模型依赖、设备执行还是服务策略造成。本阶段只建立章节与确定性事件载体，完整推导将在下一实施组展开。
